# Parking Lot — claude-harness

Drop side-quests and unplanned issues here during micro-sessions instead of derailing.

Format: `- [YYYY-MM-DD] <one-line description> (source: micro-session goal X)`

## Open

- [2026-04-19] Retire gobot-local `/close-wave` at `gobot/.claude/skills/close-wave/SKILL.md` after the new global `/close-wave` (shipped 2026-04-19 in claude-harness, symlinked to `~/.claude/skills/close-wave`) is validated against gobot Wave 3. Generic reads `.harness-profile` for `quality_gate.command` + `protected_paths`; gobot profile may need those fields added before retirement. Validation criterion: global /close-wave completes all 9 steps on gobot Wave 3 without regressions vs the gobot-local version. (source: user request to import /close-wave global, 2026-04-19)
- [2026-04-12] 🔑 OpenAI API key exposed in `~/.claude/settings.json` under the `paperbanana` MCP config — hardcoded in plain text. Move to environment variable or keychain. (source: micro — review auto-snapshot hook naming)
- [2026-04-12] Auto-snapshot hook uses generic "auto: daily snapshot" message — overwrote the intended "feat: 3-layer methodology safety net" commit message. Review whether the hook should use a different naming pattern (e.g. `wip:` or `checkpoint:`) so it's obvious which commits are auto vs intentional. May not be a bug — just a convention gap. (source: micro — check snapshot coverage)
- [2026-04-12] Skills with `disable-model-invocation: true` (session-start, session-end, park, micro) can't be invoked via the Skill tool — Claude Code rejects them. Need a workaround: either remove the flag and guard differently, or document that these skills are "agent reads SKILL.md and executes manually" only. Current UX is a confusing error when user types the skill name. (source: no active micro)
- [2026-04-11] setup-harness backups: renaming a skill dir to `<name>.bak-*` still leaves `name:` in frontmatter unchanged, so skill discovery finds duplicates. Fix A itself was fine; the cleanup step needs a rule: "when backing up a skill, either move it out of the discovery path entirely, or rewrite the name: field." Add this to Fix B (sync-harness) design. (source: /setup-harness showed 2 options after Fix A)
- [2026-04-11] Add version number to /setup-harness skill frontmatter + print it in the Step 8 summary so you can verify you're running the latest. Proposal: `version: X.Y.Z` in frontmatter, bumped on meaningful changes, echoed as first line of run output. Same pattern for every Layer 1/2 skill. Catches "am I running the old version?" confusion at a glance. (source: user after running /setup-harness in wordwideAI, wanting confidence the symlinked version was live)
- [2026-04-11] session-start should flag missing docs/plan.md: if `current_phase > Architect` AND no plan.md → emit a red-flag warning (drift — past Architect with no plan). If `current_phase = Architect` AND no plan.md → ask "run spec-planner today, or doing exploratory work?". Lightweight check only; does NOT auto-parse or auto-select tasks from plan.md (user explicitly prefers lean rituals over clever automation). (source: user Q "what happens at session-start → micro if there is/isn't already a plan for the project?")
- [2026-04-11] Document branches vs worktrees in methodology.md. Two models: (A) one session per branch — don't switch branches mid-session, short topic branches are commit containers inside a micro; (B) worktree per workstream — for `workstreams.mode: multi` projects, each workstream gets its own worktree with isolated `.harness-state/`. Worktrees share `.git` but each is its own ritual context. (source: user Q "what about git branch or worktree will the methodology follow?")
- [2026-04-11] session-start should detect cross-session branch switches: write `.harness-state/session_branch` on session open. On next session-start, if current branch ≠ session_branch → warn "You switched branches since last session. Run `session-end` first or confirm you're continuing the same work." Lightweight guard only — does NOT make skills fully branch-aware. (source: user Q about branches)
- [2026-04-11] setup-harness should recommend worktrees in its Step 8 summary when `.harness-profile` has `workstreams.mode: multi`: "Your project has N workstreams. Consider a worktree per workstream for isolated session contexts: `git worktree add ../<name> <branch>`". Zero ritual cost, high clarity for multi-workstream projects like finflow-deck. (source: user Q about branches)
- [2026-04-11] session-start should warn when working tree has >N uncommitted files at session open (source: session-start live run, pre-micro)
- [2026-04-11] session-start parking_baseline init fails when parking_lot.md has no matches — grep exits 1, breaks under strict bash; add `|| echo 0 > ...` fallback (source: session-start live run, pre-micro) [queued 2026-04-27]
- [2026-04-11] micro skill doesn't instruct agent to fetch real wall-clock time — agent hallucinated ISO timestamp + budget-end, printed wrong end-of-budget by hours. Fix: add explicit `date -u +%Y-%m-%dT%H:%M:%SZ` command to Step 4, compute budget end from that (source: paper-dry-run project-init micro, caught by user from clock mismatch) [queued 2026-04-27]
- [2026-04-27] Agent-vetting signature system — per-file indicator showing which agents have reviewed a file at which content hash, so re-runs auto-skip when signature is current and stale signatures invalidate when file changes. Two storage surfaces: (a) `reviewed_by:` block in YAML frontmatter for files that already have it (specs, agents, frontmatter docs), (b) sidecar ledger `.harness-state/review-ledger.json` keyed by repo-relative path for code/non-frontmatter files. One reader contract: agent looks up path in either surface, hash-matches, decides skip vs run. Each agent (spec-planner, codex-adversarial-review, code-reviewer, …) leaves its own signature. Hash computed over file content excluding the signature block to prevent self-invalidation. Default behavior on stale: warn-and-rerun. Surfaces in /planning-loop and /commit output as "vetted by X@h1, Y@h2, Z@h3". Run /planning-loop on the spec when ready. (source: user Q "how is it possible that after /planning-loop still code-reviewer founds issues? where is the loose end here?" — landed on two-pass model as-is, then user wanted indicator regardless to avoid re-running same agent on already-vetted file)
- [2026-04-27] Codex as second-pass code reviewer (alongside Claude `code-reviewer`) — open question: what's the right balance between code quality and token burn? Options to revisit: (1) tiered — `code-reviewer` always, Codex only on stakes-gated waves or pre-merge to protected branches; (2) disagreement-triggered — Codex runs only when `code-reviewer` returns LGTM on substantial changes (cheap second opinion on false-LGTMs); (3) dual-arbiter on every commit (rejected: token burn too high for low-stakes work). Keep `code-reviewer` as Claude (in-session context, can run tests/types, scores against `criteria/`) — Codex is additive, never replacement. **Instrument on gobot/global-proxy/wordwideAI stakes:high paths, NOT on claude-harness** — claude-harness is meta-tooling (bash + markdown), narrow bug surface, ~80% Codex-noise expected; the real signal lives in concurrency-heavy Go (gobot), TS middleware on Apple Silicon (global-proxy), and B2B/compliance code (wordwideAI). Gate is per-workstream `stakes:` in `.harness-profile`, not per-repo. Decision deferred until 5–10 stakes:high commits with both reviewers run side-by-side to measure Codex's marginal hit rate. (source: no active micro — conversation about /planning-loop ↔ code-review boundary)
- [2026-04-27] Codex onboarding gap — `/planning-loop` requires the `openai-codex` plugin to be installed and authenticated (calls `node codex-companion.mjs adversarial-review`; bails if Codex is missing or `setup --json` returns `ready: false`). Today there's no pre-flight from `/setup-harness` or `/project-init` to detect this; users install the harness, run `/planning-loop`, and hit Step 3 "Codex companion script not found" or `ready: false` mid-loop. Fix: `/setup-harness` (or `/project-init`'s post-install check) should run `node $(find ~/.claude/plugins -name codex-companion.mjs -path '*openai-codex*') setup --json` and surface the result — green "Codex ready, /planning-loop is available" or yellow "Codex not authenticated; install/configure the openai-codex plugin before using /planning-loop or /codex:adversarial-review". Same pattern usable for any future skill that depends on a plugin (gh CLI for /close-wave PR ops, jq for JSON parsing, etc.). (source: user Q during /planning-loop auto-apply spec planning — "should project-init ask if the user has an openai key before installing /planning-loop?")
- [2026-04-28] anthropic-reviews routine should bail before pushing when it has 0 relevant posts. Today (per closed-as-empty PRs #2 and #3 from 2026-04-22 and 2026-04-23) it still creates a branch, pushes, and opens a PR even when the dated suggestions file would be empty. The PR title `0 relevant, N skipped — 0 apply, 0 defer, 0 reject, 0 spec` is itself the bail signal. Fix: in the routine prompt, after triage, if `apply + defer + reject + spec == 0`, exit cleanly without pushing the branch or opening a PR — append the skipped posts to `reviewed-posts.md` only. Rationale: empty PRs are pure noise and require manual close + branch delete. Source: /apply-anthropic-reviews 2026-04-28 closed PRs #2 and #3 with that comment. (source: user-noticed during /apply-anthropic-reviews backlog clear)
- [2026-04-30] [code-review] Spec §7.1 naming — "## 7.1 Resolved decisions (Wave 0 prerequisites)" reads slightly ambiguous; could be confused with "Wave 7.1". Consider renaming to `## 7.1 Resolved (was Q5)` or moving under §7 as a "Resolved" sub-heading. Aesthetic, not blocking. (source: commit review of docs/specs/2026-04-30-harness-evolution.md)
- [2026-04-30] [code-review] Spec §0 question 5 says `→ WORKFLOW.md ladder` but the WORKFLOW.md template is a *matrix*, not a *ladder*. Either rename "ladder" to "matrix" in §0 or add an explicit "Next action ladder" section to the WORKFLOW.md template in Wave 0. Aesthetic. (source: commit review of docs/specs/2026-04-30-harness-evolution.md)
- [2026-04-30] [code-review] Plan.md Wave 7 spec link points at the roadmap spec (`./specs/2026-04-30-harness-evolution.md`); per protocol-first doctrine each wave gets its own detail spec (Wave 0 detail spec is `2026-05-01-protocol-baseline.md`, TBD). Update plan.md link once detail spec exists. Non-blocking — roadmap spec links to detail spec from §4 Wave 0 header. (source: commit review of docs/plan.md)
- [2026-04-30] [code-review] `/project-init` schema docs (`skills/project-init/SKILL.md`) need a one-bullet add for the new `protocol_baseline: bool` profile field, so future `/project-init` regeneration preserves the flag. Pattern: same as `planning_loop.auto_apply` schema-doc add (Wave 4 of model-pin spec). Schedule for Wave 0 detail spec or a follow-up. (source: commit review of docs/specs/2026-04-30-harness-evolution.md, M3 mitigation)
- [2026-05-01] [code-review] `awk '/^\|/ && /deferred decision/'` scope-to-table-rows check in plan.md Wave 8 exit gate is narrower than `grep -F` but still has loose-check edge cases: header row containing the phrase, fenced code blocks whose lines start with `|`, and legitimate cells in non-required-command rows. No notion of "required command rows" enforced. Acceptable for current Wave 8 WORKFLOW.md scope (small) but flag if the matrix grows. Better long-term: extract data-row body cells via a row-aware parser (yq on a YAML-converted matrix or a tiny awk that tracks header position). (source: round-2 commit review of plan.md Wave 8 entry)
- [2026-05-01] [code-review] Wave 8 exit-gate block duplicates content from spec §8 Wave 0; drift risk if §8 is amended. Long-term cleanup: collapse plan.md gate to "see spec §8 Wave 0 exit gate, plus: `recompute-keys.sh` returns 0" once the spec stabilizes. Not blocking — kept inline now for at-a-glance dispatch readability. (source: round-2 commit review of plan.md Wave 8 entry, NIT)
## Resolved

- [2026-05-02] **emit-receipt.sh zsh-incompat + /commit lifecycle gotcha — RESOLVED.** Both bugs surfaced during /commit on Wave 10 spec landed today. Fix 1: renamed `local status` → `local rstatus` in `emit_receipt_terminal` (write_atomic + trap handler already used `rstatus`/`cause`); added top-of-file bash-version guard that prints a clear error + exits 1 when sourced under non-bash shell. Fix 2: updated `skills/commit/SKILL.md` Step 4.5 with a CRITICAL note explaining the single-shell-session requirement + canonical `bash <<'BASH_EOF' ... BASH_EOF` wrapper pattern. Verified: 44/44 fixtures pass; bash sourcing OK; zsh sourcing now fails cleanly with the guard message. Affects every receipt-emitting skill (/commit, /run-wave, /close-wave, future /archive-plan + /harness-status).
- [2026-04-28] **/planning-loop log-writer ↔ parser format mismatch — RESOLVED.** Parker entry from this morning shipped on master (commit forthcoming this session). Parser regex at `skills/planning-loop/lib/auto-apply.sh:161` changed from requiring `(F[0-9]+):` prefix to auto-deriving F-IDs by document order (matches SKILL.md Step 6e Clause 2 contract). Backward-compat: any pre-stamped `F<n>:` prefix on a bullet is stripped from the captured title via a 3-line guard, so existing fixtures A-T still pass. New fixture U (`codex-shape-no-prefix.md`) added to lock in the auto-derive path on real-Codex bullet shape. 21/21 fixtures PASS.
- [2026-04-28] **2026-04-26 §3 effort_default re-anchor — RESOLVED.** Originally parked the same day for /spec-planner with 3 options (A: re-anchor, B: document divergence, C: per-task hint). Next-day review (2026-04-27 §1) recommended Option A with concrete edits and rationale (Anthropic's Opus-4.7 product default is xhigh; first-write-only derivation protects existing projects; cost-conscious users keep the override path). Applied as 04-27 §1 — see commit log for the 3-file edit set (skills/project-init/SKILL.md table + comment template, .harness-profile line 31). Cite anthropic-reviews/2026-04-26-improvement-suggestions.md §3 → anthropic-reviews/2026-04-27-improvement-suggestions.md §1.
- [2026-04-11] 🚨 **Propagation gap — Fix A applied.** ~/.claude/harness, ~/.claude/skills/{setup-harness,project-init,session-start,micro,park,session-end,deploy-check,api-smoke-test,migration-check,a11y-check,pivot-check}, and ~/.claude/agents/{code-reviewer,spec-planner,project-tracker}.md are now symlinks → workspace/claude-harness. Backups at .bak-20260411. **Fix B (sync-harness skill for installed projects + cross-machine sync) still open — promote if working on >1 machine or installing to >1 project.** Caveat: new symlinked skills won't appear in THIS session's skill registry; Claude Code discovers skills at startup. Next session start should pick them up.
