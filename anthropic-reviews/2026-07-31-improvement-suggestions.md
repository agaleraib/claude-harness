# 2026-07-31 — Improvement suggestions from Anthropic posts

Generated by the "review Anthropic posts" routine. Each section links to the original post and proposes a concrete change inside this repo. See `reviewed-posts.md` for the full review log (including posts that were judged out-of-scope).

> **Context for this run.** Window covered: 2026-05-11 → 2026-07-31 (since the last merged tracker entry on 2026-05-10). The window is unusually long — the prior two routines shipped branches (`claude/anthropic-review-2026-05-14` and `claude/anthropic-review-2026-06-08`) that are still **open** and haven't landed in `master`. This run is layered on top: **posts already covered by one of those open PRs are marked `skipped — covered by open PR` in the tracker and not re-litigated here**, so the two in-flight PRs remain the source of triage for the May 11 – early June span. This file focuses on posts published mid-June through end-of-July that neither open PR touched. Highlights: Claude Fable 5 / Mythos 5 (June 9) — flagship above the harness's cost profile, skipped; the June 17 **Steering Claude Code** post — a definitive decision matrix for CLAUDE.md / skills / hooks / subagents that maps directly onto how this harness is organized, cited (§2); the June 30 **Sonnet 5 launch** — Sonnet 5 becomes the Claude Code default with a 1M context, replacing the harness's `claude-sonnet-4-6` fallback pin (§1); the June 30 **Loop engineering** post — official framing of the pattern the harness's `/run-loop` skill already implements, cited (§3); the July 21 **AI-native SDLC** post from Anthropic's Deputy CISO (§4); and the July 28 **MCP 2026-07-28** spec landing in Claude, rejected (§5) because the harness has no MCP surface. All 15 in-window posts (§s + skipped) are tracked in `reviewed-posts.md`.

## Triage summary

| § | Suggestion | Recommended | Why |
|---|------------|-------------|-----|
| 1 | Update `.harness-profile` `model.fallback` from `claude-sonnet-4-6` to `claude-sonnet-5` | apply | Sonnet 5 shipped 2026-06-30 with a native 1M-token context and is now the default in Claude Code; the pinned Sonnet 4.6 fallback is superseded. Single-line config edit. |
| 2 | Add "Steering Claude Code" (2026-06-17) to the README References section | apply | Anthropic just published the definitive decision matrix for the four extension surfaces this harness uses heavily (CLAUDE.md, skills, hooks, subagents). Single-bullet addition to `README.md:1109`. |
| 3 | Cite "Loop engineering: Getting started with loops" (2026-06-30) in the `/run-loop` skill body | apply | The harness's third execution lane (`/run-loop`) implements exactly the pattern Anthropic has now branded and documented. Adding one-sentence cross-reference makes the skill discoverable from the outside framing. |
| 4 | Cite "How Anthropic secures its AI-native software development lifecycle" (2026-07-21) as external evidence for `procedures/api-security-checklist.md` and the `security-review` slash command | defer | Deputy-CISO case study on securing an SDLC where AI authors 80% of merged code — corroborates the harness's stance without requiring a specific change. Worth a one-line reference the next time `procedures/` is touched; not worth a standalone edit run. |
| 5 | Prepare `setup-harness` for the MCP 2026-07-28 stateless spec | reject — no harness MCP surface | The harness itself neither ships nor consumes MCP servers today (`grep -rn "MCP 2026\|stateless MCP" .` returns nothing; the only MCP mentions are in `examples/` and skill templates for consumer projects). The new spec matters to MCP server authors, not to this harness. Bookkeeping row kept in the tracker so future readers can trace the decision. |

**Counts:** 3 apply · 1 defer · 1 reject · 0 spec

---

## 1. Update `.harness-profile` `model.fallback` from `claude-sonnet-4-6` to `claude-sonnet-5`

**Source:** [Introducing Claude Sonnet 5 — anthropic.com/news, 2026-06-30](https://www.anthropic.com/news/claude-sonnet-5). Sonnet 5 (model ID `claude-sonnet-5`) ships with a native 1M-token context window, is now the **default model in Claude Code**, and is priced at $2/$10 per million tokens (introductory through 2026-08-31, then $3/$15).

### Why it's relevant

`.harness-profile` currently pins:

```yaml
model:
  primary: claude-opus-4-7
  fallback: claude-sonnet-4-6
  effort_default: xhigh
```

The `fallback` field is what the orchestrator drops to when a task is judged "sonnet-appropriate" (per Wave 2's effort routing) — historically `claude-sonnet-4-6`. As of 2026-06-30, Sonnet 5 supersedes 4.6 across every Claude Code surface: it's the default for Free / Pro / Team Standard, available on Max / Enterprise, and drop-in-compatible with the same model-ID mechanic. The pinned 4.6 fallback is now a **downshift** from the platform default, not an upshift as intended.

Deliberately holding the **primary** at `claude-opus-4-7` this run: the primary-pin conversation is already open in [`claude/anthropic-review-2026-06-08`](../) via that branch's §1 (Opus 4.8 evaluation, currently `defer`). That PR is the right thread for the primary decision; this § only touches the fallback where there is no ambiguity — Sonnet 5 is the strictly-better replacement for 4.6 at the same price band.

### Concrete change

**One-line edit in `.harness-profile`:**

```diff
 model:
   primary: claude-opus-4-7
-  fallback: claude-sonnet-4-6
+  fallback: claude-sonnet-5
   effort_default: xhigh   # derived from stakes.level: medium
```

No other file needs to change — the `fallback` field is a string consumed by the orchestrator, and Sonnet 5's model ID slots in wherever `claude-sonnet-4-6` was used. Existing `grep`-hits for `claude-sonnet-4-6` (per `Grep`: `docs/waves/wave1-*`, `docs/specs/2026-04-19-*`, prior `anthropic-reviews/*` files, `.harness-profile`) are either historical spec text (do NOT edit) or the config change above.

### Expected payoff

The Sonnet-tier fallback tracks the actual Claude Code default. When the orchestrator routes a task to `sonnet`, the pinned model is the one Anthropic ships as the default rather than a version behind, and consumer projects that `setup-harness` inherits pick up the modern fallback automatically. Cost: 1-line change; risk: zero — model IDs are interchangeable at the pin layer.

### Why this isn't already done

`.harness-profile:31` was written in Wave 1 (2026-04-11) and pinned to the then-current Sonnet 4.6. Nothing has bumped it since; the 2026-06-08 open PR only touches the primary discussion, not the fallback. Grep confirms: `grep -n "claude-sonnet-4-6\|claude-sonnet-5" .harness-profile` returns only the 4.6 pin.

**Verify before applying:**
1. `grep -n "claude-sonnet-4-6\|claude-sonnet-5" /home/user/claude-harness/.harness-profile` — must still show `fallback: claude-sonnet-4-6` and no `claude-sonnet-5`. If it already shows `claude-sonnet-5`, another routine has already applied this.
2. `git -C /home/user/claude-harness log --all --oneline -- .harness-profile | head -20` — if a recent commit already renames the fallback, drop this §.
3. Confirm Sonnet 5's model ID is still `claude-sonnet-5` (not `claude-sonnet-5-1` or similar) at the time of application — Anthropic sometimes ships versioned IDs after the initial launch. If a versioned ID is now canonical, use that instead.
4. Confirm the harness's orchestrator (`.claude/agents/orchestrator.md`) has no hard-coded `claude-sonnet-4-6` string that would need to change in parallel — if it does, this § needs a companion edit and re-classifies as **modest**, not trivial.

**Recommended verdict:** apply — 1-line config edit; drop-in model-ID swap; the pinned Sonnet 4.6 fallback is now strictly behind Claude Code's default. Ship via `/micro`.
**Status:** PENDING — awaiting triage in PR review

---

## 2. Add "Steering Claude Code: when to use CLAUDE.md, skills, hooks, and subagents" (2026-06-17) to the README References section

**Source:** [Steering Claude Code: when to use CLAUDE.md, skills, hooks, and subagents — claude.com/blog, 2026-06-17](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more). Anthropic's canonical decision matrix for the four steering surfaces: *"CLAUDE.md is what Claude always knows, a skill is a procedure Claude runs on demand, a hook is a rule the harness enforces no matter what Claude decides, and a subagent is a clean side-room where Claude thinks in isolation."*

### Why it's relevant

The harness is organized precisely around those four surfaces — check the top-level layout:

- `CLAUDE.md` at the repo root (24 lines of Claude-specific overrides layered on `AGENTS.md`).
- `skills/` (24 skills: `commit`, `micro`, `run-wave`, `run-loop`, `session-start`, `session-end`, `setup-harness`, `apply-anthropic-reviews`, ...).
- `.claude/settings.json` (`hooks` block; currently `{}` at the harness itself but seeded by `setup-harness` in consumer projects).
- `.claude/agents/` (subagents: `orchestrator`, `generator`, `code-reviewer`, `ui-evaluator`, `Explore`, `Plan`, `project-tracker`).

The Anthropic post is now the **canonical outside reference** for the "when to use which" question. Today the README references section (`README.md:1109`) cites older Anthropic engineering posts (harness design, effective agents, multi-agent research) but nothing on the *steering-surface decision*. A first-time reader who lands on the harness and asks "why is `/commit` a skill and not a hook?" has no post to link to.

### Concrete change

**One-line addition to the References section in `README.md`.** Insert after the existing `Skills` bullet (currently at line 1113):

```diff
 - [Custom Subagents](https://code.claude.com/docs/en/sub-agents) — Agent definition reference
 - [Skills](https://code.claude.com/docs/en/skills) — Skill system reference
+- [Steering Claude Code: when to use CLAUDE.md, skills, hooks, and subagents](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more) — Anthropic's decision matrix for the four extension surfaces this harness is organized around (June 2026)
 - [Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents) — "Start simple, add complexity only when needed"
```

### Expected payoff

The reader who asks "when should this new procedure be a skill vs a hook vs a subagent?" gets pointed at Anthropic's official framing rather than having to reverse-engineer the harness's implicit decisions. Cost: one bullet in the references section; risk: zero.

### Why this isn't a duplicate

`grep -rn "Steering Claude Code\|steering-claude-code" /home/user/claude-harness` returns nothing (verified this run). The 2026-05-10 §1 that landed in commit `8b5b12a` added a `Code Review for Claude Code` reference to the body of the `code-reviewer` agent section but did **not** touch the References section itself — verified with `sed -n '1109,1120p' README.md` (Code Review bullet is not there).

**Verify before applying:**
1. `grep -n "Steering Claude Code\|blog/steering-claude-code" /home/user/claude-harness/README.md` — must return nothing today; if it returns a hit, the bullet has been added already.
2. `sed -n '1105,1120p' /home/user/claude-harness/README.md` — must still show the References section ending at the "Context Window Visualization" bullet before the `## License` header, exactly as it does today.
3. Confirm the post URL is still live: expect the slug `steering-claude-code-skills-hooks-rules-subagents-and-more`. If the URL has since moved (Anthropic sometimes renames slugs), use the redirect target.

**Recommended verdict:** apply — trivial doc edit, single bullet in an existing list, fills a gap in the References section that will otherwise be asked aloud. Ship via `/micro`.
**Status:** PENDING — awaiting triage in PR review

---

## 3. Cite "Loop engineering: Getting started with loops" (2026-06-30) in the `/run-loop` skill body

**Source:** [Loop engineering: Getting started with loops — claude.com/blog, 2026-06-30](https://claude.com/blog/getting-started-with-loops). Anthropic's canonical framing of the pattern behind `/loop`, `/schedule`, and long-running agents. Quoting the post's own definition: *"loops are agents repeating cycles of work until a stop condition is met."* Companion piece: Boris Cherny (creator of Claude Code) publicly said in June 2026 that he no longer prompts Claude directly — *"my job is to write loops."*

### Why it's relevant

The harness ships `/run-loop` as its **third execution lane** — this is called out explicitly in `skills/run-loop/SKILL.md:9`:

> `/run-loop` is the harness's **third execution lane**. Where `/run-wave` dispatches one wave and stops, and `/close-wave` merges it, `/run-loop` drives a *stream* of ready work — plan.md waves or `ready-for-agent` gh issues — end-to-end behind a mechanical gate, unattended, until the source drains or a termination cap fires.

Same shape as the Anthropic post's framing: a loop that pulls next ready work, runs a mechanical gate, records, and repeats. The SKILL body already explains the harness's **specific** contract (auto-merge, AFK-frontier-first scheduling, host-safety guardrails); adding a one-sentence link to Anthropic's canonical post gives external readers a shared vocabulary rather than making them derive the pattern from scratch.

The linked SKILL section also carefully differentiates `/run-loop` from Anthropic's built-in `/loop` interval scheduler (`skills/run-loop/SKILL.md:15`). That comparison is more meaningful when the reader has the canonical loop-engineering post to compare against.

### Concrete change

**One-sentence addition after the "third execution lane" paragraph in `skills/run-loop/SKILL.md`** (currently ends around line 11):

```diff
 The engine is the shared, frozen-interface module at `skills/_shared/loop/`: pull next ready item → resolve its runner → run the per-item mechanical gate (implement → exit gate → code-review → bounded auto-fix → file leftover findings → atomic merge) → record → repeat. It is a pure function of (work-source state, git/issue state), so **resume is just "run again."**

 Name chosen over `/loop` — that is the Anthropic interval-scheduler built-in. `/run-loop` is consistent with `/run-wave`.
+
+> **Framing reference.** Anthropic's June 2026 post [Loop engineering: Getting started with loops](https://claude.com/blog/getting-started-with-loops) is the canonical outside framing for this pattern. `/run-loop` is the harness's opinionated variant: mechanical gate per item, source-drain termination, review-bounded auto-fix. The Anthropic post covers the general shape; the harness contract below covers the specifics.
```

### Expected payoff

The SKILL body carries its external context: a reader (or future-you) who lands on `/run-loop` sees where the pattern sits in the broader Anthropic vocabulary. Cost: one blockquote paragraph; risk: zero.

### Why this isn't a duplicate

`grep -rn "getting-started-with-loops\|Loop engineering" /home/user/claude-harness` returns nothing this run — the SKILL body doesn't currently cite the post.

**Verify before applying:**
1. `grep -n "getting-started-with-loops\|Loop engineering" /home/user/claude-harness/skills/run-loop/SKILL.md` — must return nothing today; if it hits, the cross-reference is already in place.
2. `head -20 /home/user/claude-harness/skills/run-loop/SKILL.md` — must still show the "third execution lane" prose and the "/loop is the Anthropic interval-scheduler built-in" line unchanged. If the SKILL body has been rewritten, this edit needs to attach to the new structure.
3. Confirm the post URL is still live (slug `getting-started-with-loops` on `claude.com/blog`); update the redirect target if it moved.

**Recommended verdict:** apply — trivial single-blockquote addition to a skill that already talks about the pattern; the outside reference makes the harness's opinionated variant discoverable. Ship via `/micro`.
**Status:** PENDING — awaiting triage in PR review

---

## 4. Cite "How Anthropic secures its AI-native software development lifecycle" (2026-07-21) as external evidence for `procedures/api-security-checklist.md` and the built-in `/security-review` skill

**Source:** [How Anthropic secures its AI-native software development lifecycle — claude.com/blog, 2026-07-21](https://claude.com/blog/how-anthropic-secures-its-ai-native-software-development-lifecycle). Deputy-CISO Jason Clinton on how the Security Engineering team secures an SDLC where **Claude authors ~80% of merged code**; describes external pentesting for major launches, periodic DAST scans, staged environments, and layered human oversight.

### Why it's relevant

The harness ships two security surfaces:

- `procedures/api-security-checklist.md` — framework-agnostic, "run through this for every new or modified API route." Currently a solo-maintainer checklist; no external corroborating evidence cited.
- Built-in `/security-review` skill (Claude Code) — surfaced to consumer projects via `setup-harness`.

Neither cites Anthropic's own SDLC-security guidance today. `grep -rn "AI-native SDLC\|how-anthropic-secures" /home/user/claude-harness` returns nothing. The Clinton post is the first Anthropic-published, first-party operational account of how an AI-native code lifecycle stays defensible — exactly the kind of citation that raises the perceived stakes of running the checklist for a solo maintainer.

But — this is genuinely a *bookkeeping* citation, not a change request. The checklist's items are already correct; the post doesn't add new controls the checklist is missing. Adding a "See also" reference in the `procedures/` header would be defensible but slight.

### Concrete change (deferred)

**Optional** one-line addition to `procedures/api-security-checklist.md` header block:

```diff
 # API Security Checklist

 > Framework-agnostic. Run through this for every new or modified API route, endpoint, or RPC handler.
 >
 > **When to use:** Harden phase exit gate for projects where `audience.data_sensitivity ≠ none`. Required for `financial | health | pii | regulated`. Recommended for all public-facing APIs.
 >
 > **How to use:** Read each item against the actual code path. Mark each box only if you've verified it — not if you "think it should be there." Unchecked items block phase exit.
+>
+> **External corroboration:** Anthropic's [How Anthropic secures its AI-native SDLC](https://claude.com/blog/how-anthropic-secures-its-ai-native-software-development-lifecycle) (July 2026) describes how the same class of controls is operated inside a codebase where ~80% of merged code is AI-authored — pentesting for major launches, periodic DAST scans, layered human oversight.
```

### Why this is `defer`, not `apply`

1. **Nothing is broken.** The checklist is correct; adding a link doesn't change any item.
2. **Change-batching is cheaper.** The next time `procedures/` is touched for a real reason, fold this in — a standalone commit just to add one reference bullet is heavier than it's worth.
3. **The Clinton post is a case study, not a spec.** It corroborates rather than prescribes. Waiting for a post that adds a concrete control the checklist is missing is a better trigger.
4. **Consumer projects don't need this yet.** `setup-harness` doesn't run the checklist automatically; it's read on demand when a phase-gate calls for it. The reader who needs the link is already on the page.

### Expected payoff (when applied)

A consumer-project maintainer running the checklist has an Anthropic-authoritative case-study to point at when their team asks "why do we go through this every time?" — turns the checklist from a solo opinion into a link-backed practice.

**Verify before applying:**
1. `grep -n "how-anthropic-secures\|AI-native SDLC" /home/user/claude-harness/procedures/api-security-checklist.md` — must return nothing today; if a hit, the citation is already present.
2. Confirm the post URL is still live: expect `claude.com/blog/how-anthropic-secures-its-ai-native-software-development-lifecycle`.
3. Check whether `procedures/api-security-checklist.md` has been touched for a real reason since 2026-07-31 (`git log --oneline -- procedures/api-security-checklist.md` since this file's date). If it has, fold this in on that commit rather than opening a standalone edit.

**Recommended verdict:** defer until `procedures/` is opened for another reason — bookkeeping citation; the checklist is correct as-is; the batching cost of a standalone commit outweighs the marginal value of the link today.
**Status:** PENDING — awaiting triage in PR review

---

## 5. Prepare `setup-harness` for the MCP 2026-07-28 stateless spec — reject

**Source:** [Bringing MCP 2026-07-28 to Claude — claude.com/blog, 2026-07-28](https://claude.com/blog/bringing-mcp-2026-07-28-to-claude). The stateless-core spec release: request/response protocol replacing bidirectional stateful sessions, first-class extensions (MCP Apps, Tasks, Enterprise Managed Auth), stronger OAuth/OIDC, serverless-friendly deployment. Adoption context: MCP crossed 400M monthly SDK downloads in mid-2026.

### Why it's relevant (and why it isn't)

MCP 2026-07-28 is a large protocol upgrade for **MCP server authors and MCP-consuming clients**. This harness is neither:

- `grep -rn "MCP 2026\|stateless MCP\|mcp[- ]spec" /home/user/claude-harness` returns nothing.
- `grep -rn "MCP\|mcpServers\|mcp\.json" /home/user/claude-harness` returns hits only in (a) `examples/web-app-project/.mcp.json` (an *example* for consumer projects), (b) `skills/new-cowork/templates/mcp-config-snippet.json.tmpl` (a *template* the `new-cowork` skill materializes into a consumer project), (c) `skills/skill-creator/references/anthropic-skill-authoring.md` (a vendored authoring snapshot). Nothing in the harness's own runtime consumes an MCP server; nothing in the harness *ships* one.
- `setup-harness/SKILL.md` doesn't touch MCP configuration.

The harness therefore has no direct migration to do. The spec change matters only when a consumer project decides to run an MCP server — and at that point the migration is on the server author (Claude Code auto-negotiates the protocol version on the client side per the post).

### Why this isn't `defer`

Defer implies "revisit later when conditions change." The condition that would change this to actionable is *the harness starts running an MCP server or consuming one directly* — which is a separate architectural decision, not a spec-driven follow-up. A `defer` row here would suggest the harness has an MCP concern; it does not.

### Bookkeeping value of leaving the rejection here

Future readers hitting the MCP-2026-07-28 announcement will search this file first for "MCP 2026." Finding an explicit `reject — no harness MCP surface today` row saves them from re-running the same audit. If the harness ever picks up an MCP concern, this § converts to `spec` and the trigger conditions above become the entry criteria.

**Verify before applying:**
1. `grep -rn "MCP 2026\|stateless MCP" /home/user/claude-harness` — must still return nothing. If the harness has adopted MCP since this file was written, re-open this § as a genuine migration question.
2. `grep -rn "mcpServers" /home/user/claude-harness` — a hit outside `examples/` or `skills/new-cowork/templates/` or `skills/skill-creator/references/` would signal the harness itself now runs an MCP server, in which case re-open this §.
3. Confirm the MCP spec version is still `2026-07-28` at the time of re-review. Anthropic historically ships subsequent MCP spec versions on ~quarterly cadence; the version-string may have advanced by the next routine run without changing the disposition.

**Recommended verdict:** reject — no harness MCP surface today; every MCP mention in the repo is inside consumer-project templates that the spec change doesn't force. Re-open only when the harness itself gains an MCP concern.
**Status:** PENDING — awaiting triage in PR review

---

## Summary

Five §s out of fifteen in-window posts. **§1 (apply — Sonnet 5 fallback pin)** is the most concrete change — a one-line `.harness-profile` edit that catches up the fallback to the current Claude Code default without touching the ongoing primary-pin discussion in the open 2026-06-08 branch. **§2 (apply)** and **§3 (apply)** are trivial single-line documentation citations that fill real gaps in the README references and the `/run-loop` skill body, respectively. **§4 (defer)** flags a bookkeeping citation for `procedures/api-security-checklist.md` — worth batching into the next real `procedures/` touch. **§5 (reject)** dispositions the MCP 2026-07-28 spec cleanly: no harness surface today, converts to `spec` only if the harness later grows one. All other in-window posts (Claude Fable 5 / Mythos 5, Redeploying Fable 5, Opus 4.8 [covered by open PR], Auto Mode expansion [covered by 2026-05-08 §1 APPLIED], Claude Certification, Agent SDK billing, plugin marketplace launch, Advanced tool use, Datadog customer story, Voice mode) are tracked as `skipped` with reasons in `reviewed-posts.md`. Highest-leverage suggestion to ship first: §1.
