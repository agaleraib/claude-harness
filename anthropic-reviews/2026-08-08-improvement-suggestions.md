# 2026-08-08 — Improvement suggestions from Anthropic posts

Generated by the "review Anthropic posts" routine. Each section links to the original post and proposes a concrete change inside this repo. See `reviewed-posts.md` for the full review log (including posts that were judged out-of-scope).

> **Context for this run.** Window covered: 2026-05-11 → 2026-08-08 (three months since the prior tracker entry on 2026-05-10 — the routine was paused). Cap: 15 posts considered. The window is dominated by **two new model tiers** — **Claude Sonnet 5** (2026-06-30, now Claude Code's default) and **Claude Opus 5** (2026-07-24, new flagship) — plus the introduction of the **Mythos-class `Fable 5`** (June 9), the **Claude Code Artifacts** feature (June 18), the first-party **effort level and model selection** guide (July 7), the **"How Claude Code works in large codebases"** playbook (May 14), **"How we contain Claude across products"** (May 30), and the **Cat & Thariq fireside chat** (July 21) whose Claude 5 context-engineering rules explicitly credit "unhobbling" — deleting 80%+ of Claude Code's system prompt without measurable eval loss. Governance / interpretability / campaign posts (J-space, Claude Corps, Inviting Hard Questions, Claude Science, cyber-eval incidents) are skipped with reasons in the tracker. The strongest single item is §1 (model migration) — a `spec` because it touches the pinned models in `.harness-profile`, the derivation table in `project-init`, and the routing table in `.claude/agents/orchestrator.md`; the other actionable §s are small documentation edits.

## Triage summary

| § | Suggestion | Recommended | Why |
|---|------------|-------------|-----|
| 1 | Model migration: pinned models + orchestrator routing table + README opener → Opus 5 / Sonnet 5 | spec | Touches `.harness-profile`, `skills/project-init/SKILL.md` derivation table (including the "Opus-4.7 → xhigh" postmortem justification, which no longer holds — Opus 5 defaults to `high`), `.claude/agents/orchestrator.md` routing table (all `opus-4.7`/`sonnet-4.6`/`haiku-4.5` slugs), and README's opening line. Reusable convention change; > 5 files; strategy questions (do we bump the derivation floor? keep Opus 4.7 as a fallback? migrate haiku too?) — needs `/spec-planner`. |
| 2 | Add reference to first-party "Claude Code effort level and model selection" (2026-07-07) in `skills/project-init/SKILL.md` effort docs | apply | Anthropic just published the canonical effort/model guide. The harness's current derivation table cites only the April postmortem; adding this newer link ages better and lets the maintainer sanity-check the derivation against Anthropic's own recommendations before the §1 spec lands. Pure doc edit. |
| 3 | Add "How Claude Code works in large codebases" (2026-05-14) to README `## References` | apply | The May 14 post is the first-party pairing for the README's already-stated thesis ("the harness is what matters, not just the model"). One-line bullet in the existing References section. |
| 4 | Add fireside-chat "unhobbling" evidence to README `### Why Not Superpowers` bullet list | apply | Cat & Thariq's fireside chat (July 21, transcribed by Simon Willison) reports Anthropic removed **80%+ of Claude Code's system prompt for Opus 5 / Fable 5 with no measurable coding-eval loss** by deleting constraints rather than adding few-shots. This is first-party evidence that reinforces the "keep the harness lean" bullet list; one-line addition. |
| 5 | Evaluate Claude Code Artifacts (2026-06-18) as a publishing surface for `session-end` / `project-tracker` reports | defer until an audience wants artifact links | The feature turns a Claude Code session's work into a live shareable web page (Team/Enterprise beta). The harness's `session-end` and `project-tracker` skills produce prose reports that a target-project team lead might want as a web dashboard. Not urgent for a solo harness maintainer today; flag for the day a consumer project asks. |
| 6 | Bookkeeping — cite "How we contain Claude across products" (2026-05-30) inside `sandcastle_mattpocock_architecture.md` provenance | defer until sandcastle-runner adoption widens | Anthropic's containment post codifies the same "supervise what the agent *can* do, not what it *does* do" principle Sandcastle enforces via Docker/git-worktree isolation, and puts a first-party reference behind the harness's Sandcastle notes. No behavior change; useful to have on file the next time someone asks "why do you sandbox at all when Claude Code is already sandboxed?" |

**Counts:** 3 apply · 2 defer · 0 reject · 1 spec

---

## 1. Model migration: pinned models + orchestrator routing table + README opener → Opus 5 / Sonnet 5

**Sources:**
- [Introducing Claude Sonnet 5 — anthropic.com/news, 2026-06-30](https://www.anthropic.com/news/claude-sonnet-5) — new Sonnet-class model; API id `claude-sonnet-5`; native 1M-token context; launch pricing $2/$10 per 1M tokens through 2026-08-31 (standard $3/$15 after); becomes Claude Code's default model in the same release cycle (release notes / [Claude Code v2.1.197, 2026-06-30](https://github.com/anthropics/claude-code/releases/tag/v2.1.197)).
- [Introducing Claude Opus 5 — anthropic.com/news, 2026-07-24](https://www.anthropic.com/news/claude-opus-5) — new Opus-class model; API id `claude-opus-5`; 1M-token context; pricing $5/$25 per 1M tokens (unchanged from Opus 4.8); supports the `effort` parameter; per third-party coverage aggregated for this review, current models default to `high` (not `xhigh`) on both API and Claude Code.

### Why it's relevant
The harness ships two `.harness-profile` pins and one orchestrator routing table that name specific model slugs; those slugs are now one full generation behind:

- **`.harness-profile:24-33`** — pins `model.primary: claude-opus-4-7`, `model.fallback: claude-sonnet-4-6`, and derives `effort_default: xhigh` from `stakes.level: medium` citing the 2026-04-23 postmortem's statement that "Claude Code defaults Opus-4.7 users to xhigh".
- **`skills/project-init/SKILL.md:152-165`** — the same table is the canonical schema doc distributed to consumer projects; its justification hinges on the same postmortem.
- **`.claude/agents/orchestrator.md:144-152, 245-262, 325-380`** — the runtime routing table hard-codes `haiku-4.5 / sonnet-4.6 / opus-4.7` at every complexity tier and treats `opus-4.7 + xhigh` as the terminal escalation rung.

Anthropic's newer public material now:
1. Ships Sonnet 5 as the Claude-Code default (June 30);
2. Ships Opus 5 as the new flagship (July 24) at the same price point as Opus 4.8;
3. Points readers to a fresh first-party guide (July 7, see §2) for effort/model selection that supersedes ad-hoc rules the harness derived from the April postmortem;
4. Per third-party summaries collected for this review, does **not** default Opus 5 to `xhigh` — the model defaults to `high` on both API and Claude Code, and `xhigh` is reserved for `ultrareview` / ultracode explicit invocations. If confirmed against Anthropic's own docs (see verify), the harness's `stakes.level: medium → effort_default: xhigh` derivation is now above Anthropic's own floor rather than at it, which changes the argument for the derivation.

### Concrete changes (decomposition — for `/spec-planner`)
This is one substantive change, not five small ones — every rung depends on how the spec answers the shape questions. Listing the surface area, not prescribing the answers:

- **`.harness-profile`** — bump `primary` and `fallback`; decide whether to keep Opus 4.7 as a documented rollback pin somewhere.
- **`skills/project-init/SKILL.md`** — update the schema table (default values), the derivation table, the inline template YAML, and the derivation justification (April-23 postmortem → July-7 effort/model post).
- **`.claude/agents/orchestrator.md`** — the routing table, all example JSONL lines, the escalation ladder text ("stops only at opus-4.7/xhigh" → new terminal rung), and the retry chain (`haiku-4.5 → sonnet-4.6 → opus-4.7` → new chain).
- **`README.md:3, 10, 25`** — the opening line ("designed for Opus 4.7's native capabilities"), the "Opus 4.7 plans carefully…" pitch, and the "heavy instruction systems degrade Opus 4.7 performance" line inside `### Why Not Superpowers`. Decide whether the pitch stays anchored on one flagship or generalizes.
- **`docs/specs/2026-04-19-harness-model-pin-and-effort-routing.md`** — historical spec. Do **not** rewrite; add a follow-up note or reference the new spec.
- **`anthropic-reviews/*` older files** — historical, do not touch. New tracker rows/§s point forward.

### Shape questions the spec must answer
1. Do we bump the derivation floor for `stakes.level: medium` down to `high` (matching Anthropic's new default), or keep it at `xhigh` (harness-opinionated, "we do more work than the vendor default")? Pick one and document why.
2. Does Opus 4.7 remain a valid fallback for a defined transition window, or is the migration a hard cut?
3. Does the orchestrator's retry chain still end at `opus-{max}/xhigh`, or does the addition of `Fable 5` warrant a Mythos-class terminal rung? (Fable 5 blocks in cybersecurity / bio / chem, so probably no — but the answer belongs in the spec, not this suggestion.)
4. Is `claude-haiku-4-5-20251001` still the right entry-level pin, or is there a Haiku 5 equivalent to migrate to? (Not surfaced in this review window; verify at spec time.)

### Expected payoff
The harness's pinned models age out of production support if left unchanged (Sonnet 4.6 fallback loses its 1M-context beta on 2026-04-30, already noted in the 2026-05-04 tracker skip). Migration keeps the harness on models that are still receiving product-side improvements. Secondary payoff: the derivation-table justification is no longer stale — post-migration readers see a current-year first-party reference instead of a stale April-postmortem link.

**Verify before applying:**
1. `grep -rn "opus-4-7\|sonnet-4-6\|Opus 4.7\|Sonnet 4.6" /home/user/claude-harness --exclude-dir=anthropic-reviews --exclude-dir=docs` — enumerate every remaining pin before writing the spec. Anything found is a migration target.
2. Fetch `https://platform.claude.com/docs/en/about-claude/pricing` (or the current Claude Platform pricing page) and confirm the Opus 5 / Sonnet 5 API identifiers, prices, and default effort levels **from the first-party doc** — the third-party summaries used in this review are not the source of truth. If Opus 5's default effort is `xhigh` after all, drop the "derivation floor lowering" bullet from the spec and keep the current mapping.
3. Confirm the Sonnet 5 introductory-pricing window (`$2/$10 through 2026-08-31`) has not yet elapsed at spec-write time — if standard pricing (`$3/$15`) is already in effect, adjust references in the spec.
4. Check `.claude/agents/orchestrator.md` diff since 2026-04-25 to make sure no unrelated routing changes have shipped that a mechanical migration would clobber.

**Recommended verdict:** spec — cross-file convention change with unresolved strategy questions (derivation floor, fallback retention window, Fable/Mythos handling); more than 5 files touched; concrete-changes block is a surface-area map, not a diff. Not a `/micro`.
**Status:** PENDING — awaiting triage in PR review

---

## 2. Add reference to first-party "Claude Code effort level and model selection" (2026-07-07) in the project-init effort docs

**Source:** [Claude Code effort level and model selection — claude.com/blog, 2026-07-07](https://claude.com/blog/claude-model-and-effort-level-in-claude-code). Content summary from third-party coverage: five effort levels (`low`, `medium`, `high`, `xhigh`, `max`) apply across models that support them; effort controls how much work the model does before checking back in (files read, tools used, verification loops); guidance is to tune effort as a general preference by work type, not task-by-task, and to bump effort when Claude got things wrong by skipping steps.

### Why it's relevant
`skills/project-init/SKILL.md:165` currently anchors the derivation table's authority on the [2026-04-23 postmortem](https://www.anthropic.com/engineering/april-23-postmortem) alone: *"As of Anthropic's 2026-04-23 postmortem, Claude Code defaults Opus-4.7 users to xhigh. The derivation above stays at or above that floor for the primary model."*

That statement is factually about Opus 4.7 specifically and is no longer the canonical first-party reference — Anthropic has since published a dedicated guide (July 7) that supersedes the ad-hoc postmortem-derived rule. Adding the July 7 post as a companion reference (a) ages better because the canonical page will be maintained by Anthropic, and (b) gives the §1 spec a concrete first-party doc to check the derivation against.

### Concrete change
Append a companion sentence in `skills/project-init/SKILL.md:165`:

```diff
-As of Anthropic's [2026-04-23 postmortem](https://www.anthropic.com/engineering/april-23-postmortem), Claude Code defaults Opus-4.7 users to `xhigh`. The derivation above stays at or above that floor for the primary model.
+As of Anthropic's [2026-04-23 postmortem](https://www.anthropic.com/engineering/april-23-postmortem), Claude Code defaults Opus-4.7 users to `xhigh`. The derivation above stays at or above that floor for the primary model. See also Anthropic's canonical guide on effort/model selection: [Claude Code effort level and model selection](https://claude.com/blog/claude-model-and-effort-level-in-claude-code) (2026-07-07) — the harness's derivation should be sanity-checked against this doc whenever the pinned models change (see the model-migration spec triggered by the Opus 5 / Sonnet 5 releases).
```

Optional second touch (also cheap): add a one-line bullet to the README `## References` section (`README.md:1109-1118`) — this is the same edit as §3 but for a different post; if both §2 and §3 apply, combine them into one commit.

### Expected payoff
Future readers (including the §1 spec author) hit a current-year canonical reference next to the stale postmortem link. Zero behavior change; keeps the schema doc honest about what the current first-party guidance is.

**Verify before applying:**
1. `grep -n "claude-model-and-effort-level-in-claude-code\|effort level and model selection" /home/user/claude-harness/skills/project-init/SKILL.md README.md` — must return nothing today; if it returns hits, this suggestion has already been applied.
2. Confirm the post URL (`https://claude.com/blog/claude-model-and-effort-level-in-claude-code`) still resolves and still recommends starting from default effort levels and tuning as a general preference. If Anthropic has since folded the guidance into a different canonical URL, use the new one instead.

**Recommended verdict:** apply — one-sentence documentation edit; cheap; makes the derivation-table justification current-year rather than an April back-reference; a natural companion to §1 without blocking on it.
**Status:** PENDING — awaiting triage in PR review

---

## 3. Add "How Claude Code works in large codebases" (2026-05-14) to README `## References`

**Source:** [How Claude Code works in large codebases: Best practices and where to start — claude.com/blog, 2026-05-14](https://claude.com/blog/how-claude-code-works-in-large-codebases-best-practices-and-where-to-start). Content summary from third-party coverage: Claude Code does not index the codebase into a vector database or force uploads; scope Claude Code to subdirectories rather than the monorepo root; use subdirectory-level `CLAUDE.md` files to specify commands that apply to that part of the codebase; use `.ignore` files and `permissions.deny` in `.claude/settings.json` to exclude generated files, build artifacts, and third-party code from consideration.

### Why it's relevant
The README's `### Why Not Superpowers / Heavy Skill Systems?` and `### What This Is` sections argue explicitly for a lean harness centered on the model's native capabilities. The May 14 post is the first-party pairing for that thesis at a codebase-scale angle — "the model alone is not enough; what matters is the harness—the ecosystem of files, hooks, skills, plugins, and integrations that surrounds the model and determines its behavior on the specific codebase" (paraphrased from third-party summaries). The README's `## References` section already lists harness-design posts (`Harness Design for Long-Running Apps`, `Best Practices for Claude Code`, `Building Effective Agents`); a large-codebase companion belongs on the same list.

### Concrete change
One-line bullet added to `README.md:1109-1118`:

```diff
 - [Best Practices for Claude Code](https://code.claude.com/docs/en/best-practices) — Claude Code Docs
+- [How Claude Code works in large codebases](https://claude.com/blog/how-claude-code-works-in-large-codebases-best-practices-and-where-to-start) — First-party best-practices playbook for monorepos and legacy codebases (2026-05-14); subdirectory `CLAUDE.md`, `.claude/settings.json` `permissions.deny`, `.ignore` files
 - [Extend Claude Code](https://code.claude.com/docs/en/features-overview) — Skills, Subagents, Hooks overview
```

### Optional follow-up (not part of this §)
The post's concrete recommendations (`permissions.deny`, subdirectory `CLAUDE.md`, `.ignore`) are worth a separate look for `setup-harness`. The harness has `.claude/settings.json` and subdirectory CLAUDE.md conventions but does not ship a canonical `permissions.deny` block by default (`grep -rn "permissions.deny\|permissions\\.deny" /home/user/claude-harness/.claude` returns nothing). If the maintainer wants that pattern in the harness's default settings, it's a separate spec — do not conflate with this reference addition.

### Expected payoff
Future readers see a current-year first-party reference for the "harness is what matters" thesis the README already sells. Zero behavior change.

**Verify before applying:**
1. `grep -n "how-claude-code-works-in-large-codebases\|How Claude Code works in large" /home/user/claude-harness/README.md` — must return nothing today.
2. Confirm the post URL still resolves; if the URL has moved, use the canonical Anthropic redirect.

**Recommended verdict:** apply — one-line reference addition; cheap; strengthens the References section's coverage of the harness thesis.
**Status:** PENDING — awaiting triage in PR review

---

## 4. Add fireside-chat "unhobbling" evidence to README `### Why Not Superpowers` bullet list

**Sources:**
- [A Fireside Chat with Cat and Thariq from the Claude Code team — simonwillison.net, 2026-07-21](https://simonwillison.net/2026/Jul/21/cat-and-thariq/) — transcript of the AI Engineer World's Fair chat with Cat Wu and Thariq Shihipar (Anthropic Claude Code team).
- Content summary from third-party coverage: on 2026-07-24 (same day Claude Opus 5 launched), Anthropic **removed over 80% of Claude Code's system prompt** for Opus 5 and Fable 5 **with no measurable loss on coding evals**. The framing was "unhobbling — deleting constraints that once prevented worst cases but now create conflicting instructions and wasted tokens" (paraphrased).

### Why it's relevant
The README's `### Why Not Superpowers / Heavy Skill Systems?` bullet list (`README.md:23-32`) is the harness's central thesis defense. The current bullets cite the Superpowers issue, the CLAUDE.md-ignoring issues, AGENTbench research, the April-23 postmortem, and Anthropic's own harness research. Anthropic's own Claude Code team publicly reporting that they removed 80%+ of their own product system prompt without eval regression is **strong first-party corroboration** of the same lean-context claim the section already makes — and it's more current-year evidence than the April-23 postmortem currently anchoring bullet #4.

### Concrete change
Add one bullet immediately after `README.md:31` (the "Anthropic's own conclusion" bullet), before the "Vendor primitives keep absorbing harness territory" bullet:

```diff
 - **Anthropic's own conclusion**: Their harness research removed sprint decomposition, reduced evaluator rounds, and simplified the generator — scaffolding that was essential for earlier models became counterproductive
+- **Confirmed on the vendor's own product**: Anthropic's Claude Code team publicly reported ([Cat & Thariq fireside chat, 2026-07-21](https://simonwillison.net/2026/Jul/21/cat-and-thariq/)) that they removed **over 80% of Claude Code's own system prompt for Opus 5 / Fable 5 with no measurable loss on coding evals** — framed as "unhobbling," i.e. deleting constraints that used to prevent worst cases but now create conflicting instructions and wasted tokens. The lean-context thesis holds on the vendor's own harness at least as well as it does here.
 - **Vendor primitives keep absorbing harness territory**: Claude Code now ships a built-in `/security-review` slash command, and Anthropic launched [Claude Security in public beta on 2026-04-30](https://claude.com/blog/claude-security-public-beta) with full-codebase vulnerability scanning + patch generation on Opus 4.7. A custom security-review skill would have been a reasonable addition to this harness six months ago; today it's redundant with first-party tooling. The pattern repeats — keep the harness lean so vendor releases don't deprecate it.
```

### Expected payoff
Strongest single first-party citation the harness has for its lean-context thesis. Anyone future-you or a colleague reading this section sees the argument backed by Anthropic's own product team's most recent public statement, not just research posts from earlier in the year.

**Verify before applying:**
1. `grep -n "unhobbling\|cat-and-thariq\|fireside chat" /home/user/claude-harness/README.md` — must return nothing today; if it returns hits, this suggestion has already been applied.
2. Confirm the Simon Willison transcript URL still resolves. If Anthropic has since published a first-party recap or the video's official transcript at a canonical URL, prefer that over Simon's write-up — but Simon's version is the citable one until then.
3. Sanity-check the "80%+" figure against Anthropic's own public statements before quoting it. If the exact percentage has been walked back or clarified, adjust the wording (e.g., "large majority") and drop the specific number.

**Recommended verdict:** apply — one-bullet documentation edit; the strongest first-party evidence the harness has for its central thesis; keeps the section current-year rather than April-anchored.
**Status:** PENDING — awaiting triage in PR review

---

## 5. Evaluate Claude Code Artifacts (2026-06-18) as a publishing surface for `session-end` / `project-tracker` reports

**Source:** [Anthropic brings Artifacts to Claude Code (Announcement summary as reported by The Decoder / VentureBeat) — 2026-06-18](https://the-decoder.com/anthropic-brings-artifacts-to-claude-code-letting-teams-share-live-pages-from-coding-sessions/). Content summary from third-party coverage: Claude Code Artifacts are single, self-contained interactive HTML pages published from a Claude Code session to a private URL on claude.ai; render in any browser; update in place as the session continues (version history included); available on Team/Enterprise subscriptions and both the CLI and the desktop app (v1.13576.0+); source formats `.html`, `.htm`, `.md`. From the terminal, `Ctrl+]` reopens the most recent artifact.

### Why it's relevant
The harness's `session-end` skill produces prose exit reports; `project-tracker` produces prose Second Brain entries. Both currently live inside the terminal or Second Brain and are not directly shareable as a page. Claude Code Artifacts is Anthropic's official surface for turning a Claude Code session's output into a live shareable dashboard — a natural fit for a maintainer who wants to hand a target-project team lead a "here's what this session did" URL rather than a scroll of terminal output.

### Why this isn't `apply` today
- This harness is solo-maintained (`team.size: solo` in `.harness-profile`); no one else needs the artifact URL right now.
- Artifacts require a Team/Enterprise subscription; the maintainer's plan may not include it.
- The harness's session-end + project-tracker skills work well enough via prose today; there's no active pain point.

### Recommendation
Flag for the day a consumer project asks for shareable session dashboards, or the day the maintainer's plan includes Artifacts. When triggered, the natural first target is `session-end` (the session-summary artifact is the most reusable one to publish) — do not scope the work to include `project-tracker` in the same spec unless Second Brain sync explicitly wants it.

**Verify before applying:**
1. `grep -rn "Claude Code Artifact\|artifacts-in-claude-code\|Ctrl+\\]" /home/user/claude-harness` — should return nothing today; if it returns hits, someone has already started integrating.
2. Confirm Artifacts is still Team/Enterprise-gated at apply time; if it has graduated to Pro/Max, the "no one needs it yet" objection weakens.
3. Confirm the harness's target audience (per `.harness-profile`'s `audience.kind`) still includes anyone besides the maintainer — a solo-only harness stays with prose exit reports.

**Recommended verdict:** defer until an audience wants artifact links — no active pain point today; not zero-cost to build; not needed for a solo harness.
**Status:** PENDING — awaiting triage in PR review

---

## 6. Bookkeeping — cite "How we contain Claude across products" (2026-05-30) inside `sandcastle_mattpocock_architecture.md` provenance

**Source:** [How we contain Claude across products — anthropic.com/engineering, 2026-05-30](https://www.anthropic.com/engineering/how-we-contain-claude). Authors (per third-party summaries): Max McGuinness, Mikaela Grace, Jiri De Jonghe, Jake Eaton, Abel Ribbink. Content summary: articulates a "blast radius" framework for containing agents; documents claude.ai (gVisor + ephemeral containers), Claude Code (OS-native Seatbelt on macOS / Bubblewrap on Linux), Claude Cowork (full VMs); notes that between mid-2025 and January 2026 Anthropic received three responsible-disclosure vulnerabilities in Claude Code's pre-consent code path, and that a February 2026 red-team test succeeded 24/25 times at exfiltrating `~/.aws/credentials` when Claude Code was pointed at a prompt injection. Framing principle: "supervise what the agent is *able* to do rather than what it actually does."

### Why it's relevant
The harness ships `sandcastle_mattpocock_architecture.md` — a review of Matt Pocock's `@ai-hero/sandcastle` package (Docker + git worktree isolation for parallel Claude Code runs) — as the citable reference for the `run-loop` skill's sandcastle runner. The Anthropic containment post codifies the same "cap the blast radius via deterministic environment boundaries" principle from a first-party angle and provides a citable framing (`gVisor` / `Seatbelt` / `Bubblewrap` / VMs, `blast radius`) that the harness's sandcastle notes currently do not link to.

### Concrete change
Add one line to `sandcastle_mattpocock_architecture.md` under the existing `## Provenance — what I reviewed` section (or a new `## Related first-party principle` heading if the file's structure prefers), citing the Anthropic post:

```diff
 ## Provenance — what I reviewed

 - **Source:** `git clone https://github.com/mattpocock/sandcastle` (full source, not a dist tarball).
 - **Commit:** `d7a41c828495d571a3837a980ae167dd595e4a18` — "Merge pull request #813 from mattpocock/changeset-release/main", dated 2026-06-16.
 - **Package version:** `@ai-hero/sandcastle` **0.9.0** (`package.json`).
 - **Local trial config** (public API surface): `~/workspace/sandcastle-trial/.sandcastle/main.mts` (calls `run({ sandbox, agent, promptFile, maxIterations, branchStrategy: { type: "merge-to-head" }, copyToWorktree, hooks })`).
+- **Related first-party principle:** Anthropic's [How we contain Claude across products (2026-05-30)](https://www.anthropic.com/engineering/how-we-contain-claude) frames Claude-Code's OS-native sandboxing (Seatbelt / Bubblewrap) around a "blast radius" concept and the rule "supervise what the agent is *able* to do rather than what it actually does". Sandcastle's Docker-container + git-worktree pattern is one implementation of that rule at the harness layer, above Claude Code's per-process sandbox.
 - All file:line citations below are against the cloned tree at `/tmp/sandcastle-src`.
```

### Why it isn't `apply` today
The sandcastle notes work fine without the citation. Adding it is a bookkeeping refinement, not a bug fix, and the sandcastle runner isn't widely adopted yet (`run-loop` is the only consumer). Defer until sandcastle runner adoption widens or the file is being edited for another reason.

**Verify before applying:**
1. `grep -n "how-we-contain-claude\|blast radius\|Seatbelt\|Bubblewrap" /home/user/claude-harness/sandcastle_mattpocock_architecture.md` — must return nothing today; if it returns hits, the citation is already in place.
2. Confirm the sandcastle notes file still exists and is still the citable reference for the sandcastle runner (i.e. it hasn't been moved into `docs/` or a wave-specific location).

**Recommended verdict:** defer until sandcastle-runner adoption widens — cheap doc edit, but not urgent. Small bookkeeping improvement; skip today's cycle.
**Status:** PENDING — awaiting triage in PR review

---
