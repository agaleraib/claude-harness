# 2026-08-28 — Improvement suggestions from Anthropic posts

Generated by the "review Anthropic posts" routine. Each section links to the original post and proposes a concrete change inside this repo. See `reviewed-posts.md` for the full review log (including posts that were judged out-of-scope).

> **Context for this run.** Window covered: **2026-05-11 → 2026-08-28** — a **~15-week backlog** since the previous tracker entry (2026-05-10). Fifteen posts are tracked this run (routine cap). Highlights: the **Claude 5 generation launched** across four announcements (Opus 4.8 on 2026-05-28, Fable 5 + Mythos 5 on 2026-06-09, Sonnet 5 on 2026-06-30, Opus 5 on 2026-07-24) — those are consolidated into **§2** rather than four separate §s because the concrete harness change is one coordinated `.harness-profile` + orchestrator-routing update. Two methodology-heavy posts also landed: **"Steering Claude Code"** (2026-06-18, §1) is the canonical framing for CLAUDE.md/skills/hooks/subagents/plugins/MCP that this harness already implements in fragments, and **"The new rules of context engineering for Claude 5"** (2026-07-24, §3) is the paired Claude-5 authoring guide. Anthropic also launched an official **plugin marketplace** on 2026-05-22 (§4) — the harness distributes via symlinks today, so this is a distribution-model question, not a break-fix. Later posts include an internal **on-call CI/CD agent** case study (§5), a **cross-surface memory** post (§6, corroborates existing architecture), and a **Datadog MCP-tool-design writeup** (§7). Remaining in-window posts are skip rows in the tracker with reasons.
>
> **Egress caveat.** This session's network policy blocked direct HTTPS fetches to `anthropic.com`, `claude.com`, and `resources.anthropic.com`. The URLs below are the canonical paths as indexed by Google search (surfaced by the discovery agent), not fetched-and-confirmed 200s in this session. Each § has a "Verify before applying" line that includes URL verification when the maintainer picks up the suggestion.

## Triage summary

| § | Suggestion | Recommended | Why |
|---|------------|-------------|-----|
| 1 | Adopt "Steering Claude Code" as the canonical mechanism-selection reference in `AGENTS.md` + `README.md` | apply | Definitive Anthropic framing of when to use CLAUDE.md / skills / hooks / subagents / plugins / MCP — the harness already implements the matrix in fragments; citing the post + adding a two-sentence pointer costs almost nothing and fills a real gap. |
| 2 | Refresh the harness for the Claude 5 generation (`.harness-profile` + orchestrator routing table + comments) — 4 model posts consolidated | spec | Bumping `model.primary`/`model.fallback` touches `.harness-profile`, `.claude/agents/orchestrator.md` (routing table + escalation rung, 19 model references), `skills/project-init/SKILL.md`, and README references — plus Opus 5's supported effort tiers may not match Opus 4.7's, so the `effort_default` derivation comment needs re-thinking. >5 file edits + a design question about effort levels → spec before code. |
| 3 | Adopt "The new rules of context engineering for Claude 5" as the Claude-5 authoring reference for skills/agents | defer until §2 lands | Companion guidance to the Claude 5 launch. Only actionable once `.harness-profile` actually pins Claude 5 — the guide describes behaviors of Claude 5 models, so applying it while `model.primary` is still `claude-opus-4-7` is premature. |
| 4 | Consider publishing the harness itself as a Claude Code plugin (via the May 2026 marketplace) | defer until distribution pain warrants | Anthropic shipped an official plugin directory + marketplace on 2026-05-22 (55+ vetted + community). The harness's current distribution model (`setup-harness` symlinks into `~/.claude/skills/`) works for a solo maintainer; plugin conversion is a distribution shift, not a break-fix. Flag for later. |
| 5 | Bookmark Anthropic's Claude-Tag-as-CI/CD-first-responder case study for a future on-call/PR-triage skill | defer until CI/CD pain warrants | The harness has `run-loop`, `commit`, and `code-reviewer` for the pre-push loop but no on-call agent. The Anthropic post is a strong reference for what one looks like; no code change today. |
| 6 | "Claude's memory works everywhere" corroborates the existing cross-surface memory architecture in `AGENTS.md §Cross-surface consumption` | reject — already in place | The harness has a well-developed cross-surface memory model (6-entity `~/.claude/memory/` root + `.claude/desktop-knowledge/` bundle + `AGENTS.md §Memory`); the new post confirms Anthropic's direction is compatible. No change needed unless the post surfaces a specific new mechanism that breaks the existing model — verify step below. |
| 7 | Bookmark the Datadog "universal machine tool for Claude Code" post as MCP-tool-design reference | defer until harness publishes an MCP server | The harness does not publish an MCP server today (it ships skills via symlink). Post is a strong reference for tool-shape design if that ever changes. |

**Counts:** 1 apply · 4 defer · 1 reject · 1 spec

---

## 1. Adopt "Steering Claude Code" as the canonical mechanism-selection reference

**Source:** [Steering Claude Code: when to use CLAUDE.md, skills, hooks, subagents, and more — claude.com/blog, 2026-06-18](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more)

### Why it's relevant

The harness implements every mechanism the post covers — CLAUDE.md (present at root), 25+ skills under `skills/`, 6 agents under `.claude/agents/`, hooks (documented in `README.md:889` "Hooks (Deterministic Automation)"), and MCP (mentioned in `skills/setup-harness/SKILL.md:145`). What it lacks is a **canonical mapping** from "the thing you want to steer" → "which mechanism to reach for." Today a reader learns the answer by reading five separate README sections plus `AGENTS.md §Cross-surface consumption`. Anthropic's June 2026 post is exactly that mapping, from the platform vendor's own voice — the missing anchor.

`grep -rn "steering-claude-code\|steering.claude.code" /home/user/claude-harness` returns nothing today; the post is uncited anywhere in the repo.

### Concrete change

**Two touches:**

1. **`README.md:1109` References section** — append one bullet immediately after the existing "Extend Claude Code" line:

   ```diff
    - [Extend Claude Code](https://code.claude.com/docs/en/features-overview) — Skills, Subagents, Hooks overview
   +- [Steering Claude Code: when to use CLAUDE.md, skills, hooks, subagents, and more](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more) — Anthropic's canonical framing of the mechanism-selection matrix (June 2026); the harness implements each mechanism in fragments — this is the anchor for choosing which to reach for.
    - [Custom Subagents](https://code.claude.com/docs/en/sub-agents) — Agent definition reference
   ```

2. **`AGENTS.md §What to do`** (starts line 39) — add one bullet at the end of the ordered list linking to the same post:

   ```diff
    5. Treat `docs/specs/` as durable intent. Treat `docs/plan.md` as the active board.
   +6. When choosing a mechanism (CLAUDE.md vs skill vs hook vs subagent vs plugin vs MCP), consult [Steering Claude Code](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more) — Anthropic's canonical selection matrix. This harness implements every mechanism the post covers; the post is the anchor for choosing between them.
   ```

Both are documentation-only. No code, no skill body, no configuration change.

### Expected payoff

Any future reader (or future-you) who asks "should this be a skill or a hook?" or "should this be in CLAUDE.md or in a subagent?" hits a canonical answer before making it up. Solo maintainer benefit: forestalls one of the most common design-drift questions.

### Why this isn't a duplicate

The README's existing References cites platform docs (Extend Claude Code, Custom Subagents, Skills, Best Practices) — those are **reference material**, not decision guidance. Anthropic's own decision matrix has never been cited from this repo before.

**Verify before applying:**
1. Confirm URL `https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more` returns 200 and matches the post title/date noted here. Egress in the review session was blocked; the URL is search-indexed, not fetched-and-confirmed.
2. `grep -rn "steering-claude-code" /home/user/claude-harness` — must return nothing today; if it returns hits, this § is stale.
3. `grep -c "Steering Claude Code" README.md AGENTS.md` — must return `0` on both files; if either returns non-zero, one or both of the two touches has already landed.

**Recommended verdict:** apply — pure documentation edit, two lines total, fills a real anchor gap the harness has today.
**Status:** PENDING — awaiting triage in PR review

---

## 2. Refresh the harness for the Claude 5 generation (`.harness-profile` + orchestrator routing table)

**Sources (four posts, consolidated):**
- [Introducing Claude Opus 4.8 — anthropic.com/news, 2026-05-28](https://www.anthropic.com/news/claude-opus-4-8)
- [Claude Fable 5 and Claude Mythos 5 — anthropic.com/news, 2026-06-09](https://www.anthropic.com/news/claude-fable-5-mythos-5)
- [Introducing Claude Sonnet 5 — anthropic.com/news, 2026-06-30](https://www.anthropic.com/news/claude-sonnet-5)
- [Introducing Claude Opus 5 — anthropic.com/news, 2026-07-24](https://www.anthropic.com/news/claude-opus-5)

### Why it's relevant

Since the last tracker entry (2026-05-10) Anthropic has shipped an entire model generation. From this session's own system prompt: *"The most recent Claude models are the Claude 5 family and Haiku 4.5. Model IDs — Fable 5: `claude-fable-5`, Opus 5: `claude-opus-5`, Sonnet 5: `claude-sonnet-5`, Haiku 4.5: `claude-haiku-4-5-20251001`."*

Current harness state:
- `.harness-profile:30-31` — `model.primary: claude-opus-4-7`, `model.fallback: claude-sonnet-4-6`
- `.claude/agents/orchestrator.md:146-152, 245-246` — routing table hardcodes `sonnet-4.6`, `opus-4.7`, `haiku-4.5` (19 references total, per `grep -c "opus\|sonnet\|haiku" .claude/agents/orchestrator.md`)
- `.harness-profile:26` — comment: *"Aligned with Anthropic's 2026-04-23 postmortem (Claude Code defaults Opus-4.7 users to xhigh)"* — postmortem is now three model generations stale
- `skills/project-init/SKILL.md`, README.md, and 8+ historical spec/wave files carry the same pinned model IDs

`grep -rn "claude-opus-5\|claude-sonnet-5\|opus 5\|sonnet 5" /home/user/claude-harness --include="*.md" --include=".harness-profile"` returns **zero hits** — the Claude 5 generation is entirely uncited in the harness.

### Why this is `spec`, not `apply`

Four reasons:

1. **File count.** A minimal bump touches `.harness-profile` (1), `.claude/agents/orchestrator.md` (19 references, routing table + escalation rung), `skills/project-init/SKILL.md` (derivation comments), `README.md` (agent example table + effort-derivation prose), `AGENTS.md` (comment update), plus historical files that document the current pin (`docs/specs/2026-04-19-harness-model-pin-and-effort-routing.md`, wave summaries). >5 files → substantive per the routine's promotion signals in `anthropic-reviews/README.md`.
2. **Effort-tier semantics may have changed.** The discovery agent noted Opus 5 launched with an *"effort toggle: low/med/high"* framing. The harness pins `effort_default: xhigh` because Opus 4.7 shipped it. If Opus 5 no longer supports `xhigh` (or renames the tier), the routing table's escalation rung (`sonnet-4.6 → opus-4.7` at xhigh) breaks silently. This is a **design question**, not a rename.
3. **The 2026-04-23 postmortem citation in `.harness-profile:26` is stale.** Any bump must decide whether to (a) drop the citation, (b) replace it with the Opus-5 announcement's guidance, or (c) leave it as historical. That's a small but real design call.
4. **Fable 5 is the new "Claude Code Fast mode" model** per this session's system prompt (*"Fast mode for Claude Code uses Claude Opus with faster output … It can be toggled with /fast and is available on Opus 5/4.8."*). Whether the harness should document Fable-5-as-fast-mode as an orchestrator option is another design call.

### Concrete change (deferred to spec)

Spec brief (for whoever picks this up):
1. Decide the pinned models: `model.primary`, `model.fallback`. Plausible defaults: `claude-opus-5` / `claude-sonnet-5`.
2. Confirm Opus 5's supported effort tiers. If `xhigh` is dropped, redesign `effort_default` derivation in `.harness-profile:22-25` and `skills/project-init/SKILL.md`.
3. Update `.claude/agents/orchestrator.md` routing table (line 146+) and escalation rung (line 245+).
4. Update `.harness-profile:26` postmortem citation.
5. Decide whether Fable-5-as-fast-mode gets a routing rule (probably not for xhigh workloads; possibly for `--fast` overrides).
6. Bump all `claude-opus-4-7`/`claude-sonnet-4-6` references in README, AGENTS, project-init SKILL.
7. Historical specs/waves stay pinned to their historical model — do not backport.
8. Add a References entry in README linking Opus 5, Sonnet 5, Fable 5 announcements.

### Expected payoff (when applied)

- Orchestrator dispatches to the current-generation models, capturing whatever agentic-use gains Claude 5 landed.
- `.harness-profile` reflects current guidance rather than an April 2026 postmortem.
- Consumer projects that install via `setup-harness` inherit the current model pin instead of a superseded one.

**Verify before applying:**
1. Confirm each of the four source URLs returns 200 and the model IDs match (`claude-opus-5`, `claude-sonnet-5`, `claude-fable-5`, `claude-opus-4-8`). Egress in the review session was blocked; model IDs above come from this session's system prompt.
2. `grep -c "claude-opus-4-7" .harness-profile` — must return `1` today; if `0`, the bump has already been applied and this § is stale.
3. Confirm Opus 5's supported effort tiers (low/med/high vs low/med/high/xhigh) before rewriting the `effort_default` derivation — pricing/effort surface is the most likely piece to have moved between the discovery agent's snapshot and application time.
4. Check `code.claude.com/docs/en/changelog` between 2026-05-11 and application time for any model-deprecation notices that would compress the migration window.

**Recommended verdict:** spec — three model generations of change; effort-tier semantics may have shifted; >5 files touched. Route to `/spec-planner` before any code.
**Status:** PENDING — awaiting triage in PR review

---

## 3. Adopt "The new rules of context engineering for Claude 5" as the Claude-5 authoring reference

**Source:** [The new rules of context engineering for Claude 5 generation models — claude.com/blog, 2026-07-24](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models)

### Why it's relevant

Companion post to the Opus 5 launch (same day). Per the discovery agent's summary, it covers system prompts, Skills, CLAUDE.md, and memory for the Claude 5 generation — directly touching how the harness authors its skills and agents. If the harness bumps to Claude 5 (§2), skill/agent authoring guidance in `skills/skill-creator/`, `skills/setup-harness/`, `AGENTS.md`, and the code-reviewer/spec-planner agents may all warrant a pass.

`grep -rn "the-new-rules-of-context-engineering\|context engineering for Claude 5" /home/user/claude-harness` returns nothing today.

### Why this is `defer`

Only actionable **after** §2 lands. If §2 stays pinned to Claude 4.7 (Opus) / Claude 4.6 (Sonnet), the Claude-5 guidance is inapplicable. Applying it to a 4.x pinned harness would introduce authoring rules that don't match the model actually running skills, and might silently regress skill quality.

If §2 is rejected or long-deferred, §3 is rejected in step.

### Concrete change (deferred)

When picked up:
1. Read the post; extract concrete rules that differ from `skills/skill-creator/`'s current guidance.
2. Update `skills/skill-creator/SKILL.md` + `skills/skill-creator/references/anthropic-skill-authoring.md` with the Claude-5 deltas.
3. Update `AGENTS.md §What to avoid` bullet on prompt-caching (line 54) if the post surfaces new prefix-stability guidance for Claude 5.
4. Add References entry in README pointing at the post.

### Expected payoff (when applied)

Skill and agent authoring in this repo — and every consumer project that installs via `setup-harness` — inherits Claude 5-tuned guidance rather than defaulting to Claude 4-era heuristics.

**Verify before applying:**
1. Confirm URL `https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models` returns 200. Egress-blocked in the review session.
2. Confirm §2 has landed (`grep "claude-opus-5" .harness-profile` returns a hit). If §2 is still open, this § stays deferred.
3. `grep -rn "new-rules-of-context-engineering" /home/user/claude-harness` — must return nothing at application time; hits mean already applied.

**Recommended verdict:** defer until §2 lands — companion guidance to a model bump this repo hasn't taken yet; premature to apply against a Claude 4.x-pinned harness.
**Status:** PENDING — awaiting triage in PR review

---

## 4. Consider publishing the harness itself as a Claude Code plugin

**Source:** [Customize Claude Code with plugins — anthropic.com/news, 2026-05-22](https://www.anthropic.com/news/claude-code-plugins)

### Why it's relevant

Anthropic launched an official plugin directory + community marketplace on 2026-05-22 (per the discovery agent: 55+ vetted plugins at launch). The harness's current distribution model — `setup-harness` copies/symlinks files into `~/.claude/skills/` and `~/.claude/agents/` — pre-dates this. A plugin would let consumer projects declare `claude-harness` as a dependency in `.claude/settings.json` and get skills/agents/hooks provisioned via the standard Claude Code mechanism rather than a bespoke install script.

Current state:
- `skills/setup-harness/SKILL.md` is the install path. Its two-step flow (user-level install + per-project install) works but requires bash execution.
- `grep -rn "plugin\|marketplace" /home/user/claude-harness --include="*.md" -l` returns 2 hits: `README.md:281` mentions the `openai-codex` plugin as an example, and skill-creator references discuss third-party plugins. No harness-as-plugin conception.

### Why this is `defer`

1. **Solo maintainer.** Publishing as a plugin adds a release cadence (semver, versioned tarball, marketplace listing) the current symlink-in-user-config model doesn't need.
2. **Distribution model shift, not break-fix.** `setup-harness` works today for every consumer project the maintainer has stood up.
3. **Plugin manifest format is new (May 2026).** Wait a quarter for the manifest + install semantics to settle before committing.
4. **Marketplace review model unclear.** Whether the harness would be listed vetted vs community-marketplace is a policy call.

### Concrete change (deferred)

When/if the maintainer picks this up:
1. Read the plugin manifest schema and marketplace submission process.
2. Decide vetted-vs-community submission target.
3. Convert `skills/` + `.claude/agents/` + hook config into a plugin manifest.
4. Version the release (semver).
5. Migrate `skills/setup-harness/SKILL.md` to `install claude-harness` semantics + keep the symlink path as a fallback for the first release cycle.
6. Update README's Setup section and INSTALL-FOR-COLLEAGUE.md.

### Expected payoff (when applied)

- Consumer projects add the harness with one line in `.claude/settings.json` instead of running an install script.
- Skill/agent updates arrive via the plugin update flow, not a `git pull` in `~/.claude/harness/`.
- Public discoverability via the marketplace.

**Verify before applying:**
1. Confirm URL `https://www.anthropic.com/news/claude-code-plugins` returns 200. Egress-blocked in the review session.
2. Confirm the plugin manifest schema is documented publicly at `code.claude.com/docs/en/plugins` (or similar) at application time. Pre-1.0 schemas often churn.
3. `grep -rn "claude-plugins\|marketplace" /home/user/claude-harness --include="*.md" -l` — should still return only the openai-codex mention today; hits elsewhere mean the migration is underway.
4. Confirm marketplace submission does not require Anthropic-owned account (versus solo publisher) — solo-maintainer viability check.

**Recommended verdict:** defer until distribution pain warrants — symlink install works; plugin conversion is a shift, not a fix. Flag for later when the maintainer wants public discoverability or plugin-native update flow.
**Status:** PENDING — awaiting triage in PR review

---

## 5. Bookmark Anthropic's on-call agent case study for a future CI/CD-triage skill

**Source:** [Claude on call: How Claude Tag serves as Anthropic's first responder for CI/CD failures — claude.com/blog, 2026-08-17](https://claude.com/blog/ai-ci-cd-on-call)

### Why it's relevant

Per the discovery agent's summary, this post walks through Anthropic's internal Claude Tag deployment as a first-responder for CI/CD failures — memory, Datadog/Grafana tool integrations, per-turn steering. That's a mature reference architecture for what an on-call agent looks like inside a Claude Code harness.

Current harness state:
- `run-loop` skill drives streams of ready work behind a mechanical gate (see `AGENTS.md:91`).
- `commit` + `code-reviewer` cover the pre-push loop.
- **No on-call / CI-triage agent** exists (`grep -rn "on-call\|first responder\|CI/CD failure" /home/user/claude-harness --include="*.md"` returns 1 unrelated hit in `docs/specs/2026-05-20-cowork-envelope-interview.md`).

### Why this is `defer`

1. **No CI/CD pain today.** The harness itself doesn't have CI running against `agaleraib/claude-harness` in a way that surfaces flaky-test triage as a bottleneck. The `subscribe_pr_activity` machinery Claude Code provides is already good enough for the current PR volume.
2. **On-call agent design is substantial.** Memory strategy, tool integrations (Datadog, Grafana, logging), per-turn steering, escalation policy — this is a full agent, not a small skill.
3. **The Anthropic post is a reference, not a portable pattern.** Their setup depends on their observability stack; harness would need equivalent stubs.

### Concrete change

None this run. Bookmark note for when CI-triage becomes a bottleneck.

### Expected payoff (when applied)

If the maintainer later stands up an on-call skill/agent for a consumer project, the Anthropic post is the first reference to consult — beats reinventing the shape.

**Verify before applying:**
1. Confirm URL `https://claude.com/blog/ai-ci-cd-on-call` returns 200 and title matches. Egress-blocked in the review session.
2. `grep -rn "on-call\|first responder\|CI/CD failure" /home/user/claude-harness --include="*.md" -l` — should return only the 1 unrelated hit noted above (`docs/specs/2026-05-20-cowork-envelope-interview.md`). Extra hits mean the on-call surface has grown and this § is either done or stale.
3. Re-check whether CI pain has surfaced in the maintainer's daily flow — this is the trigger to promote from `defer` to `spec`.

**Recommended verdict:** defer until CI/CD triage pain warrants — bookmark note; no code change requested this run.
**Status:** PENDING — awaiting triage in PR review

---

## 6. "Claude's memory works everywhere" corroborates the existing cross-surface memory architecture

**Source:** [Claude's memory works everywhere, and you decide what's in it — claude.com/blog, 2026-08-25](https://claude.com/blog/claudes-memory-works-everywhere-and-you-decide-whats-in-it)

### Why it's relevant

Per the discovery agent's summary, Anthropic published a cross-surface memory model with user-controlled scoping (Aug 2026). The harness has spent Waves 11 (memory-system-redesign / shared root), 13 (memory-system-redesign), and 15 (cowork area sync) building exactly this. `AGENTS.md §Cross-surface consumption` (line 83) and `AGENTS.md §Memory` (line 56) document the harness's six-entity `~/.claude/memory/` root, cross-surface bytes-equivalent bundle at `.claude/desktop-knowledge/`, manual-promotion-only discipline, and Claude-Code-vs-Desktop-vs-web parity.

`grep -rn "MEMORY\.md\|per-cwd auto-memory" /home/user/claude-harness --include="*.md" -l` returns 3 hits (`AGENTS.md`, `CLAUDE.md`, `skills/shared-root-init/SKILL.md`) — the architecture is well-developed.

### Why this is `reject`

1. **Already in place.** The harness's cross-surface memory system is Wave-11/13/15-shipped, spec-anchored (`docs/specs/2026-05-13-memory-system-redesign.md`), and operating in production. Adding a citation for corroborating Anthropic guidance is bookkeeping, not a change.
2. **No specific mechanism mismatch surfaced by the post.** The discovery agent's summary mentions user-controlled scoping — the harness's `USER.md`/`FEEDBACK.md`/`PROJECTS.md`/`REFERENCES.md`/`archive/`/`feedback/` structure already supports scoping. Unless the post surfaces a specific new mechanism the harness lacks (e.g. a canonical scope-boundary API, a new Claude-side memory-consent gesture), there's nothing concrete to import.
3. **The verify step below is the actual work.** If the post *does* surface a new mechanism the harness lacks, that becomes a new § — but rejecting this § doesn't lose that path.

### Concrete change

None. Reject.

### Expected payoff of the reject

Decision-trail integrity. Without this entry, a reader of the 2026-05-13 memory-redesign spec has no way to know that Anthropic subsequently published a corroborating cross-surface memory model. The tracker row + this § close the loop.

**Verify before applying:**
1. Confirm URL `https://claude.com/blog/claudes-memory-works-everywhere-and-you-decide-whats-in-it` returns 200. Egress-blocked in the review session.
2. Once fetchable, re-read the post to check whether it surfaces a specific new mechanism (a new memory scope, a consent-gesture API, a scope-boundary declaration) that the harness's `AGENTS.md §Cross-surface consumption` does not yet accommodate. If it does, spin a new §; do not reopen this one.
3. `grep -n "claudes-memory-works-everywhere\|memory works everywhere" /home/user/claude-harness` — should return nothing today (post uncited). If a reader has cited it elsewhere as a corroborating source, this § can be updated to say "cited in commit X" and moved to APPLIED-as-citation-only.

**Recommended verdict:** reject — already in place; existing `AGENTS.md §Cross-surface consumption` + Wave-11/13/15 architecture covers the pattern. Revisit only if the post surfaces a specific new mechanism.
**Status:** PENDING — awaiting triage in PR review

---

## 7. Bookmark the Datadog "universal machine tool" post as MCP-tool-design reference

**Source:** [How Datadog built a "universal machine tool" for Claude Code — claude.com/blog, 2026-07-21](https://claude.com/blog/how-datadog-built-a-universal-machine-tool-for-claude-code)

### Why it's relevant

Per the discovery agent's summary, Datadog published MCP-tool-design methodology from a mature integrator — the kind of "one tool that dispatches to N sub-operations" pattern that shows up whenever a team ships their first MCP server. The harness doesn't ship an MCP server itself today (skills/setup-harness/SKILL.md:145 mentions MCP allowlist seeding via `/fewer-permission-prompts`; no server implementation).

`grep -rn "Datadog\|universal machine\|universal machine tool" /home/user/claude-harness --include="*.md" -l` returns 0 hits.

### Why this is `defer`

1. **The harness has no MCP-server publishing surface today.** Skills are the primary distribution; `.mcp.json` shows up in `examples/web-app-project/` as a consumer-project config, not a harness-authored server.
2. **The Datadog pattern is server-author guidance.** It's directly applicable *when* the harness (or a consumer project) authors its own MCP server. Not before.
3. **Solo-maintainer scope.** Publishing an MCP server is the kind of thing a plugin would first make sense (see §4). Both are deferred.

### Concrete change

None this run. Bookmark for when the harness or a consumer project authors an MCP server.

### Expected payoff (when applied)

If the maintainer later spins up an MCP server (e.g. for a wordwideAI backend, or as part of a `harness-status`-style aggregator across projects), the Datadog post is the first reference to consult for tool-shape design.

**Verify before applying:**
1. Confirm URL `https://claude.com/blog/how-datadog-built-a-universal-machine-tool-for-claude-code` returns 200. Egress-blocked in the review session.
2. `grep -rn "Datadog\|universal machine tool" /home/user/claude-harness --include="*.md" -l` — should return 0 today. If a reader cites the post in a consumer project's spec, this § can be updated to APPLIED-as-citation.
3. Confirm no in-flight spec proposes a harness-authored MCP server (`grep -rn "MCP server" docs/specs/`) — if one exists, promote this § from bookmark to `spec` input.

**Recommended verdict:** defer until harness or a consumer project authors an MCP server — reference material for tool-shape design; not applicable to the current symlink-based distribution model.
**Status:** PENDING — awaiting triage in PR review

---

## Summary

Fifteen posts reviewed across a **~15-week backlog** (2026-05-11 → 2026-08-28). Seven §s cover nine posts (the four Claude 5 launches are consolidated into §2); the remaining six posts are skip rows in `reviewed-posts.md` with reasons.

**Highest-leverage suggestion to ship first:** §1 — the "Steering Claude Code" citation. Two lines, no code, fills a real anchor gap.

**Biggest suggestion the maintainer needs to schedule:** §2 — the Claude 5 model bump. Not a rename; effort-tier semantics may have moved. Route to `/spec-planner` when the maintainer has spec-writing time.

**Bookkeeping notes:** §3 blocks on §2; §4/§5/§7 are defer-with-bookmark for later distribution/CI/MCP-server transitions; §6 is the corroborating-Anthropic-guidance reject.

**Egress caveat:** all Anthropic URLs in this file are search-indexed, not fetched-and-confirmed in this session. Each § carries a URL-verify step in "Verify before applying." Confirm 200 before quoting titles/dates in downstream artifacts.
