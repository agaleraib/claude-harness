# 2026-06-04 — Improvement suggestions from Anthropic posts

Generated by the "review Anthropic posts" routine. Each section links to the original post and proposes a concrete change inside this repo. See `reviewed-posts.md` for the full review log (including posts that were judged out-of-scope).

> **Context for this run.** Window covered: 2026-05-11 → 2026-06-04 (since the prior tracker entry on 2026-05-10). The window is dominated by two anchor releases — **Claude Opus 4.8** (anthropic.com/news, 2026-05-28) and the **"How we contain Claude across products"** engineering deep-dive (anthropic.com/engineering, same day) — plus the **Code w/ Claude London 2026** keynote (2026-05-19) that shipped **self-hosted sandboxes** + **MCP tunnels** into Managed Agents public beta, the **CLUE Triage** customer-engineering story (claude.com/blog, ~2026-05-12), and a wave of partnership/customer announcements (Gates Foundation $200M, KPMG global alliance, Stainless acquisition, 28 new compliance integrations) that are out-of-scope for a coding harness. Of the in-window posts, three produce actionable §s; the rest are skipped with reasons in `reviewed-posts.md`. The `apply` recommendation is concentrated on §1 — the Opus 4.8 cutover — because the harness pins `claude-opus-4-7` in `.harness-profile`, names "Opus 4.7" throughout README, and references `opus-4.7` in the orchestrator's retry-rung.

## Triage summary

| § | Suggestion | Recommended | Why |
|---|------------|-------------|-----|
| 1 | Cut over `.harness-profile` + README + orchestrator + project-init from Opus 4.7 → Opus 4.8 and re-evaluate the `effort_default` derivation now that Opus 4.8 ships with **`high`** (not `xhigh`) as Anthropic's surface default | spec | Touches `.harness-profile`, `README.md` (multiple lines), `.claude/agents/orchestrator.md` (retry rung), `skills/project-init/SKILL.md` (schema + derivation comment), and `skills/apply-anthropic-reviews/SKILL.md` (the example bullet uses 4.7). >5 file edits + an effort-derivation semantic question; route through `/spec-planner`. |
| 2 | Cite **"How we contain Claude across products"** as the engineering deep-dive reference next to the existing Auto Mode prose in `skills/setup-harness/SKILL.md` and the README "Why Not Superpowers" evidence list | apply | Pure documentation edit; the engineering post is the canonical companion to the May 2026 Auto Mode product post the harness already references. ~2 link bullets + 1 sentence; fills the only remaining gap in the existing containment narrative. |
| 3 | Bookkeeping: **self-hosted sandboxes + MCP tunnels** in Managed Agents stacks more corroborating evidence onto the deferred Managed Agents chain (2026-04-19 §2, 2026-04-25 memory row, 2026-05-07 §1, 2026-05-10 §3) | defer until Managed Agents adoption | Pattern parallel: agent loop on Anthropic infra + tools on user-owned sandbox is the cloud analogue of this harness's "Claude Code session on user machine + project state in `.harness-state/`". No concrete change requested; the durable record gains one more corroborating data point. |

**Counts:** 0 apply (direct) · 1 defer · 0 reject · 1 spec · **1 apply for §2 (light documentation edit)**

(§1 routes through spec-planner because the model swap is large and the effort-derivation semantics need a deliberate design pass. §2 is the only direct-apply this run.)

---

## 1. Opus 4.7 → Opus 4.8 cutover (model pin, README prose, orchestrator rung, project-init schema)

**Source:** [Introducing Claude Opus 4.8 — anthropic.com/news, 2026-05-28](https://www.anthropic.com/news/claude-opus-4-8). Companion engineering coverage of the **dynamic workflows** research preview shipped alongside: [Orchestrate subagents at scale with dynamic workflows — code.claude.com/docs](https://code.claude.com/docs/en/workflows). API model ID: `claude-opus-4-8`. Same `$5/$25 per MTok` pricing as Opus 4.7. Fast mode now $10/$50 per MTok (3× cheaper than fast mode was on Opus 4.7).

### Why it's relevant

This harness is pinned hard to Opus 4.7. Every surface that pins, names, or describes the model needs to be updated:

- `.harness-profile:30` — `model.primary: claude-opus-4-7` (the harness's own profile).
- `.harness-profile:26` — comment block says "Aligned with Anthropic's 2026-04-23 postmortem (Claude Code defaults Opus-4.7 users to xhigh)." That alignment statement is now **dated** — see the effort-derivation question below.
- `skills/project-init/SKILL.md:133` — schema table row: `model.primary | yes | claude-opus-4-7 | Top-of-stack model...`.
- `skills/project-init/SKILL.md:187` — template YAML emits `primary: claude-opus-4-7`.
- `skills/project-init/SKILL.md:183` — comment: "Aligned with Anthropic's 2026-04-23 postmortem (Claude Code defaults Opus-4.7 users to xhigh)."
- `skills/project-init/SKILL.md:146` — prose: "As of Anthropic's 2026-04-23 postmortem, Claude Code defaults Opus-4.7 users to xhigh."
- `.claude/agents/orchestrator.md:246` — retry-rung promotes `sonnet-4.6 → opus-4.7`. With Opus 4.8 GA, the rung's top tier becomes `opus-4.8`.
- `README.md:3, 10, 25, 818, 857` — multiple prose hits naming "Opus 4.7" as the design target.
- `skills/apply-anthropic-reviews/SKILL.md:69` — the worked example bullet shows `"Opus 4.6" → "Opus 4.7"`. That example was real once (the 2026-04-19 §1 cutover); we'd swap it to the new worked example so the apply-skill's worked example stays current.

### The new effort-derivation question (this is why it's `spec`, not `apply`)

The current derivation:

| `stakes.level` | `effort_default` |
|---|---|
| `low` | `high` |
| `medium` | `xhigh` |
| `high` | `xhigh` |

was [explicitly anchored](skills/project-init/SKILL.md#L146) to **Opus 4.7's Claude Code surface default of `xhigh`** (per the 2026-04-23 postmortem the harness cites). As of 2026-05-28, **Opus 4.8 defaults to `high` on every surface — claude.ai, API, and Claude Code** (see the Opus 4.8 announcement and corroborating reporting on the new five effort levels: `low | medium | high | xhigh | max`). The anchor moved.

There are at least three coherent positions to take, and the right one is a design choice the maintainer should make explicitly:

1. **Hold the floor at `xhigh` for medium/high stakes regardless of model.** The derivation was always *above* Claude Code's default, not equal to it; the postmortem floor logic still applies. Update the comment to drop the 4.7-specific framing but keep the table.
2. **Snap the derivation to the new Opus 4.8 default.** `stakes.level: medium → high`; `stakes.level: high → xhigh`. Tighter alignment with the surface default; risks under-spending on medium-stakes work that the prior derivation would have caught at `xhigh`.
3. **Introduce the `max` tier.** Opus 4.8 added a fifth tier above `xhigh`. The orchestrator's rung today is `low → medium → high → xhigh → promote-model → stop`. Adding `max` would change the rung to `low → medium → high → xhigh → max → promote-model → stop`. Touches the orchestrator's Step 6 retry logic and the Surface A/B logging schemas.

Spec-planner should pick one and document why. §1 itself names the question; the answer lives in `docs/specs/<date>-opus-4-8-cutover.md`.

### Concrete change (deferred to /spec-planner)

The §1 cutover is too broad for a single `/micro` instruction. It mixes a mechanical find-and-replace with a semantic design decision (the derivation question above) plus a multi-file orchestrator update. Per the apply-anthropic-reviews routine's decompose test in `anthropic-reviews/README.md#L60-77`, four promotion signals fire:

- (1) **Shape underspecified** — derivation question has multiple coherent answers.
- (2) **Touches in-flight mechanism** — the effort-routing rung is an active orchestrator code path.
- (3) **Introduces or modifies a reusable convention** — the `effort_default` derivation is consumed by every project's profile via `project-init`.
- (4) **>5 file edits** — confirmed above (8 distinct file/location hits).

Bullets to feed `/spec-planner`:

- §1a — `.harness-profile`: swap `model.primary` to `claude-opus-4-8`; update the postmortem-citation comment to reflect the new surface default. Re-evaluate `effort_default: xhigh` against the chosen derivation.
- §1b — `README.md`: replace "Opus 4.7" with "Opus 4.8" at lines 3/10/25/818/857; refresh the [harness-design-long-running-apps](https://www.anthropic.com/engineering/harness-design-long-running-apps) framing if the Opus 4.8 announcement materially shifts what the model now does well at low effort.
- §1c — `.claude/agents/orchestrator.md`: update the retry-rung's `sonnet-4.6 → opus-4.7` line to `sonnet-4.6 → opus-4.8`; decide whether to add `max` as a fifth effort tier above `xhigh` (Position 3 above) and ripple through the Surface A/B logging shapes and JSONL schema.
- §1d — `skills/project-init/SKILL.md`: schema table row (`model.primary` default), template YAML (`primary: claude-opus-4-8`), the two prose citations of the 2026-04-23 postmortem, and the derivation table.
- §1e — `skills/apply-anthropic-reviews/SKILL.md`: swap the §1a worked-example bullet so the apply-skill's worked example matches a real recent cutover (this very § when shipped).
- §1f — `anthropic-reviews/README.md`: nothing to change here, but spec-planner should sanity-check whether the routine's discovery scope or the `apply / defer / reject / spec` rubric needs a Opus-4.8-flavored example.
- §1g — Decide whether to mention the **dynamic workflows research preview** in `README.md` and `.claude/agents/orchestrator.md` as a "complementary not redundant" structural parallel to the harness's local orchestrator-worker pattern (analogous to the 2026-05-10 §1 treatment of `/ultrareview` vs. local `code-reviewer`).

### Expected payoff

- `.harness-profile` pin is honest about which model the harness is designed for.
- The orchestrator routes to the model that actually exists.
- The effort derivation is anchored to the current surface default, not a 4.7-era citation that's already stale.
- The apply-anthropic-reviews worked example stays current — the §1a/§1b/§1c expansion in `skills/apply-anthropic-reviews/SKILL.md#L67-71` is a teaching artifact and should not point at a dead worked example.
- (If spec-planner picks Position 3) The orchestrator gains a `max` rung that matches Opus 4.8's exposed effort tiers.

### Verify before applying

1. `grep -rn "claude-opus-4-7\|opus-4\.7\|Opus 4\.7" .harness-profile README.md skills/ .claude/agents/ docs/specs/2026-04-19-harness-model-pin-and-effort-routing.md` — must still return the 8 location hits enumerated above; if any have already been migrated, drop those bullets from the spec. Today (2026-06-04) returns hits across `.harness-profile:30`, `skills/project-init/SKILL.md:133/146/183/187`, `skills/apply-anthropic-reviews/SKILL.md:69`, `.claude/agents/orchestrator.md:246`, and `README.md:3/10/25/818/857`.
2. `grep -n "claude-opus-4-8\|Opus 4\.8" .harness-profile README.md skills/ .claude/agents/` — must return zero today; if it returns hits, a partial cutover is already underway and this § needs to be rebased on the partial state.
3. Confirm Opus 4.8's surface default is still `high` (not silently re-defaulted to `xhigh`) on the day of the cutover. The announcement is dated 2026-05-28; defaults can move post-launch — spec-planner re-checks before locking the derivation.
4. Confirm Opus 4.8 has not been renamed (e.g., to `claude-opus-4-8-2026-05-28` like some other Anthropic model IDs have suffix-versioned). Check `https://platform.claude.com/docs/en/release-notes/overview` for the canonical model ID before pinning.
5. `grep -n "claude-sonnet-4-6" .claude/agents/orchestrator.md` — confirm the Sonnet rung hasn't moved (Sonnet 4.6 was still current as of this window; no Sonnet 4.7 has shipped). If a Sonnet upgrade landed between this review and the cutover, the §1c rung update needs to ripple to that too.

**Recommended verdict:** spec — multi-file model cutover + effort-derivation semantic question + orchestrator rung change. Touches >5 files (8 confirmed locations) and at least one design choice has multiple coherent answers. Goes to `/spec-planner` per `anthropic-reviews/README.md#L52-58` ("Substantive" row of the size-routing table).
**Status:** PENDING — awaiting triage in PR review

---

## 2. Cite "How we contain Claude across products" alongside the existing Auto Mode prose

**Source:** [How we contain Claude across products — anthropic.com/engineering, ~2026-05-28](https://www.anthropic.com/engineering/how-we-contain-claude). The post is the engineering companion to the May 2026 Auto Mode launch (already cited in `skills/setup-harness/SKILL.md#L139-149` and `anthropic-reviews/2026-05-08-improvement-suggestions.md#L1`). It documents containment patterns across claude.ai, Claude Code, and Cowork — sandboxes, virtual machines, egress controls, and a defense-in-depth model where Auto Mode's classifier is **one layer** of several. It also discloses a February 2026 internal red-team exercise in which a phished Claude Code session exfiltrated AWS credentials 24/25 times when only the Auto Mode classifier stood between the prompt and the network; the egress allowlist was what actually contained the attack.

### Why it's relevant

The harness already documents Auto Mode + hooks layering in `skills/setup-harness/SKILL.md#L139-149`:

> Claude Code's Auto Mode (GA on Pro/Max/Team/Enterprise/API as of May 2026) runs a Sonnet 4.6 classifier that auto-approves safe tool calls and surfaces only the risky ones. **Hooks run before the classifier**, so anything this harness allow/deny-lists in `.claude/settings.json` (and anything `fewer-permission-prompts` adds for you) is honored as a hard rule; the classifier only sees the residual.

That paragraph is correct but only covers Auto Mode in isolation. The engineering post is the canonical reference for **why the classifier alone isn't sufficient** — the 24/25 phish stat is exactly the kind of evidence the harness's "Why Not Superpowers" section uses elsewhere (cf. the April 23 postmortem citation at `README.md#L30`). One link, one sentence.

The `README.md#L23-32` "Why Not Superpowers" evidence list also picks up a useful sixth bullet: **containment-by-egress-allowlist is now a documented first-party defense pattern** for solo coding harnesses running inside sandboxed containers. The harness doesn't ship a sandbox, but a future reader who's deciding whether to enable Auto Mode in a `bypassPermissions` container has a real first-party post to read.

### Concrete change

**One bullet appended to the existing `Permission model — Auto Mode + hooks layering` section in `skills/setup-harness/SKILL.md`** (currently lines 139-149). Insert after the closing line `If you work in an isolated container, prefer Auto Mode over bypassPermissions — Auto Mode keeps the classifier's prompt-injection mitigations active.`:

```diff
 If you work in an isolated container, prefer Auto Mode over `bypassPermissions` — Auto Mode keeps the classifier's prompt-injection mitigations active.
+
+**Reference:** Anthropic's engineering deep-dive [How we contain Claude across products (2026-05-28)](https://www.anthropic.com/engineering/how-we-contain-claude) documents containment as defense-in-depth — the Auto Mode classifier is one layer; egress controls, sandbox filesystem boundaries, and trust-dialog timing each catch failures the others miss. The post discloses a February 2026 internal red-team exercise where a phished Claude Code session exfiltrated AWS credentials 24 times out of 25 attempts when only the classifier was in the path — the egress allowlist was what actually contained the attack. Treat the harness's `.claude/settings.json` hooks block as one layer in that chain; on multi-account or production-credential machines, pair it with a sandbox or container that enforces network egress separately.
```

**One bullet appended to the README "Why Not Superpowers" evidence list** at `README.md#L32` (after the "Vendor primitives keep absorbing harness territory" bullet, before the "**This harness keeps context overhead under 500 tokens at session start**" paragraph):

```diff
 - **Vendor primitives keep absorbing harness territory**: Claude Code now ships a built-in `/security-review` slash command, and Anthropic launched [Claude Security in public beta on 2026-04-30](https://claude.com/blog/claude-security-public-beta) with full-codebase vulnerability scanning + patch generation on Opus 4.7. A custom security-review skill would have been a reasonable addition to this harness six months ago; today it's redundant with first-party tooling. The pattern repeats — keep the harness lean so vendor releases don't deprecate it.
+- **Containment is first-party**: Anthropic's [How we contain Claude across products (2026-05-28)](https://www.anthropic.com/engineering/how-we-contain-claude) treats sandbox + egress allowlist + classifier + hooks as defense-in-depth — a 24/25 AWS-credentials phish exercise showed the classifier alone is not sufficient. The harness's role is the hooks layer; the operator's environment must provide the sandbox + egress controls. Don't build a competing classifier-in-prompt.
```

### Expected payoff

The harness's containment story is consistent across surfaces: `setup-harness` says "use Auto Mode + hooks", the README says "we deliberately don't build a classifier-in-prompt", and both link to the same first-party reference for the deeper why. A future reader who lands on either page can follow one link to the engineering deep-dive; no duplication of the post's content, no re-deriving the threat model in the README.

### Why this isn't a duplicate of 2026-05-08 §1

That § was about the **Auto Mode engineering deep-dive** (the product launch post). This one is about a **separate** engineering post that frames Auto Mode as one component of a layered model — and quotes a hard number (24/25 phish success) that the existing prose doesn't carry. The two posts are complementary; citing the May 28 post does NOT duplicate the May 8 §.

### Verify before applying

1. `grep -rn "how-we-contain\|How we contain Claude" /home/user/claude-harness` — must return zero today; if it returns hits, the citation has already been added and this § is stale. Confirmed zero on 2026-06-04.
2. `grep -n "AWS credentials\|24 out of 25\|February 2026 internal red-team" /home/user/claude-harness` — must return zero today; the 24/25 stat is direct from the engineering post and shouldn't already be quoted anywhere in the repo.
3. Confirm the engineering post URL still resolves (`https://www.anthropic.com/engineering/how-we-contain-claude`) and the AWS-credentials red-team example is still the canonical incident the post leads with — if Anthropic edits the post to remove the disclosed incident, drop the 24/25 sentence and keep just the defense-in-depth framing.
4. Confirm `.claude/settings.json` is still the canonical hook configuration file in the next Claude Code release — the May 2026 release notes added `skillOverrides` next to the existing hooks block, but the hooks block itself remains the harness's layer in this defense chain.

**Recommended verdict:** apply — light documentation edit (~1 paragraph in `setup-harness/SKILL.md` + 1 bullet in README); fills the only remaining gap in the existing Auto Mode + containment narrative; quotes a first-party number that strengthens the "Why Not Superpowers" thesis without duplicating any earlier §.
**Status:** PENDING — awaiting triage in PR review

---

## 3. Bookkeeping: Managed Agents now ship self-hosted sandboxes + MCP tunnels

**Source:** [New in Claude Managed Agents: self-hosted sandboxes and MCP tunnels — claude.com/blog, 2026-05-19](https://claude.com/blog/claude-managed-agents-updates) (Code w/ Claude London 2026 launch). Self-hosted sandboxes moved to **public beta**; MCP tunnels remain **research preview** with a request-access gate. The pattern: tool execution moves to user-controlled infrastructure (Cloudflare / Daytona / Modal / Vercel or self-hosted), while the orchestration loop stays on Anthropic infra.

### Why it's relevant

This is the **fourth** in-window Managed Agents post that lands on the same parking spot as the chain of prior deferrals:

- `2026-04-19 §2` (brain/hands split for the orchestrator) — `Status: DEFERRED`.
- `2026-04-25` Managed Agents memory row — `Status: skipped`.
- `2026-05-07 §1` (Managed Agents dreaming/outcomes/orchestration) — `Status: DEFERRED`.
- `2026-05-10 §3` (Dreaming bookkeeping) — `Status: DEFERRED`.
- **(this run)** Self-hosted sandboxes + MCP tunnels — **another corroborating data point**, same disposition.

The architectural shape Anthropic is shipping is now very close to the harness's own runtime model: agent loop on one machine, tool execution on a different machine, both coordinating through a network boundary. In the harness's case, the "different machine" is the operator's laptop (where the Claude Code session runs) and the "loop" is the same process; in Managed Agents, the loop is on Anthropic's hosted runtime and the tool execution is on user-controlled infra. The structural parallel is real — but the harness is solo-coding-focused and doesn't run on Managed Agents today, so the disposition is unchanged.

### Why this is `defer`, not a new ask

The conditions that have kept the prior deferrals deferred haven't moved:

1. The harness still targets a solo Claude Code user — no Anthropic-hosted runtime.
2. Self-hosted sandboxes are public beta, MCP tunnels are research preview; neither has a Pro/Max free-tier surface.
3. The setup cost of moving the harness's coordination to a Managed Agents runtime hasn't been justified by any current pain point; the harness's local `.harness-state/` continues to cover cross-session memory at zero marginal cost.

### Concrete change

**None this run.** This § exists so the durable record reflects that the Managed Agents stack has continued to ship features through May 2026, all of which corroborate (but do not promote) the deferred 2026-04-19 §2 chain. When/if the maintainer wants to harvest the pattern, the cheapest concrete experiment is the same one §3 from 2026-05-10 sketched ("a `patterns.md` append-only memory store") — not a Managed Agents migration.

If the maintainer ever moves on the brain/hands split (deferred 2026-04-19 §2), the **MCP tunnel** primitive is the right reference for the network boundary between the orchestrator and worker subagents — the harness already separates Surface A (human log) from Surface B (machine JSONL) in `orchestrator.md`, and MCP tunnels are the production-grade version of "subagent calls a tool over a network without exposing the tool publicly."

### Expected payoff (no-op path)

Decision-trail integrity. A future reader of the 2026-04-19 §2 in 2026-Q4 sees that **five** independent posts now corroborate the same Managed Agents pattern (Effective harnesses for long-running agents, Managed Agents memory, Managed Agents dreaming, self-hosted sandboxes, MCP tunnels). No mid-2026 promotion happened; the deferral is intentional and well-evidenced.

### Verify before applying

1. `grep -n "DEFERRED" anthropic-reviews/2026-04-19-improvement-suggestions.md` should still show §2 (brain/hands split) deferred. If §2 has been promoted between this review and the next one, this § is moot.
2. `grep -n "Managed Agents" anthropic-reviews/reviewed-posts.md` should still show the chain of `skipped` / `relevant→deferred` rows. If a row has been re-classified to `applied`, the chain has been broken and the corroborating-evidence framing needs to be reread.
3. `grep -rn "self-hosted sandbox\|MCP tunnel\|claude-managed-agents-updates" /home/user/claude-harness` — must return zero today; if it returns hits, partial Managed Agents adoption has begun and the deferral is no longer the active position. Confirmed zero on 2026-06-04.
4. Re-check whether self-hosted sandboxes has graduated from public beta to GA between this review and the next, and whether MCP tunnels remains gated. If either has flipped to free-tier-accessible, the cost calculus in the 2026-04-19 §2 chain shifts and spec-planner should re-evaluate.

**Recommended verdict:** defer until Managed Agents adoption — bookkeeping note only; no concrete change requested; the linked 2026-04-19 §2 chain remains DEFERRED with five corroborating posts now stacked behind it.
**Status:** DEFERRED until Managed Agents adoption — bookkeeping; self-hosted sandboxes + MCP tunnels stack onto the existing 2026-04-19 §2 + 2026-04-25 + 2026-05-07 §1 + 2026-05-10 §3 chain · 2026-06-04

---

## Summary

Three §s from a moderately busy 25-day window. **§1 (spec)** is the highest-leverage change of the run — Opus 4.8 shipped on 2026-05-28 and the harness pins to Opus 4.7 across 8 confirmed locations, plus the `effort_default` derivation references a 4.7-specific Claude Code default that has moved. Spec-planner needs to make a deliberate call on whether the derivation tracks Anthropic's new `high` default, holds the floor at `xhigh`, or adds the new `max` tier; the file edits then ripple from that decision. **§2 (apply)** is the only direct-apply: a ~1-paragraph citation of the "How we contain Claude across products" engineering deep-dive next to the existing Auto Mode prose in `setup-harness/SKILL.md` and a matching bullet in the README "Why Not Superpowers" list — fills the only gap in the existing containment narrative without duplicating the 2026-05-08 §1 Auto Mode coverage. **§3 (defer)** is the durable-record bookkeeping note: the Managed Agents stack continues to ship features (self-hosted sandboxes public beta, MCP tunnels research preview) — fifth corroborating data point on the 2026-04-19 §2 brain/hands deferral chain, no promotion. The remaining 11+ in-window posts (CLUE Triage customer story, Gates Foundation $200M, KPMG global alliance, Stainless acquisition, 28 compliance integrations, partner-Opus-cybersecurity story, finance-team narrative, sales-leader Cowork story, Office add-ins, both Code w/ Claude city recaps) are skipped with one-line reasons in `reviewed-posts.md`. Highest-leverage suggestion to ship first: **§2** (small + applies today), then route §1 through `/spec-planner` before any 4.8 commit lands.
