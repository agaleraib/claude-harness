# 2026-07-25 — Improvement suggestions from Anthropic posts

Generated by the "review Anthropic posts" routine. Each section links to the original post and proposes a concrete change inside this repo. See `reviewed-posts.md` for the full review log (including posts that were judged out-of-scope).

> **Context for this run.** Window covered: 2026-05-11 → 2026-07-25 — a ~76-day gap since the prior tracker entry on 2026-05-10 (routine paused, resumed today). The window brackets **three model releases** (Opus 4.8 on May 28, Fable 5 + Mythos 5 on Jun 9, Sonnet 5 on Jun 30), the Fable 5 export-control episode (Jun 12 → Jun 30 redeploy), and a cluster of methodology posts in July that map directly onto harness surfaces: **large-scale migrations** (Krieger, Jul 16), **verification loops as skills** (Jul 22), **loop engineering: getting started** (~Jun 30 / Jul 1), the **field guide to Fable 5** ("Finding your unknowns", Jul 6), and **the new rules of context engineering for Claude 5** (Jul 24). Also in-window: **How we contain Claude across products** (May) — surfaces a `.claude/settings.json` PreToolUse-hook-runs-before-consent vulnerability directly relevant to `setup-harness`. Cap: 15 posts reviewed this run. Recommended: **5 apply · 2 defer · 0 reject · 0 spec**.

## Triage summary

| § | Suggestion | Recommended | Why |
|---|------------|-------------|-----|
| 1 | Bump `.harness-profile` + `project-init` derivation to Claude Opus 4.8 (primary) + Sonnet 5 (fallback); leave Fable 5 as opt-in for the hardest specs | apply | Three model gens shipped in the window; today's pins (Opus 4.7 + Sonnet 4.6) are 60+ days behind the vendor defaults. Fable 5 is 2× Opus price and gated by a separate safety-fallback surface — not a baseline. |
| 2 | Add one bullet + one reference to README "Why Not Superpowers" citing **The new rules of context engineering for Claude 5** (Jul 24) | apply | First-party post says Anthropic removed ~80% of Claude Code's system prompt for advanced models. That's the harness's exact thesis with a fresh, load-bearing citation — one sentence + reference line. |
| 3 | Add one line to the README **Safety Check Skills** header framing `deploy-check` / `api-smoke-test` / `migration-check` / `a11y-check` as the harness's *verification loops*, citing **Building verification loops in Claude Code with skills** (Jul 22) | apply | Anthropic's post names the pattern the harness already uses. Naming it explicitly + linking makes the header readable to a stranger who's only met the pattern through the blog post. |
| 4 | Add one bullet to README References citing **How Anthropic runs large-scale code migrations with Claude Code** (Krieger, Jul 16) as first-party evidence for the wave / planning-loop discipline | apply | Krieger's 3-discarded-runs + phase-gate + adversarial-review harness is exactly the loop shape this repo built. First-party evidence is worth banking as one bullet even without any structural change. |
| 5 | Add a **"security note on trust-prompt races"** to `skills/setup-harness/SKILL.md` and to `INSTALL-FOR-COLLEAGUE.md`, citing **How we contain Claude across products** (May) | apply | Post documents a live vulnerability: `.claude/settings.json` hooks committed to a repo run *before* the trust-prompt appears. Setup-harness installs global hooks and touches project settings — a one-paragraph note that a repo you don't trust yet shouldn't be opened with Claude Code is cheap insurance. |
| 6 | Enrich `spec-planner`'s discovery phase with the **"Unknowns" four-quadrant framework** from Thariq Shihipar's Fable 5 field guide (Jul 6) | defer until a spec pass, or the next planning-loop miss | Framework (Known-Known / Known-Unknown / Unknown-Known / Unknown-Unknown) is compelling but implementation shape is fuzzy: does it change the interview script? Add a new phase? Ship as a `references/` snapshot? Belongs behind a `/spec-planner` design pass, not a `/micro`. |
| 7 | Cross-check `/run-loop`'s implicit loop taxonomy against the 4-type split (turn / goal / time / proactive) from **Loop engineering: Getting started with loops** (~Jun 30 / Jul 1) | defer until next `/run-loop` iteration | The harness's `/run-loop waves`/`issues` is a *goal-based* loop and `/planning-loop` is a *turn-based* loop with a cap. Post's taxonomy doesn't suggest a new loop; it just gives us shared vocabulary. Worth remembering when we next touch either skill; no free-standing change today. |

**Counts:** 5 apply · 2 defer · 0 reject · 0 spec

---

## 1. Bump `.harness-profile` + `project-init` derivation to Claude Opus 4.8 (primary) + Sonnet 5 (fallback)

**Sources:**
- [Introducing Claude Opus 4.8 — anthropic.com/news, 2026-05-28](https://www.anthropic.com/news/claude-opus-4-8) — model id `claude-opus-4-8`, same pricing as 4.7 ($5 / $25 per Mtok), dynamic-workflows research preview for Claude Code, effort controls, fast-mode 2.5× speed at ⅓ prior price. Positioned as a drop-in successor to 4.7 with better agentic judgment and ~4× fewer overlooked code flaws.
- [Introducing Claude Sonnet 5 — anthropic.com/news, 2026-06-30](https://www.anthropic.com/news/claude-sonnet-5) — model id `claude-sonnet-5`, launch pricing $2 / $10 per Mtok through 2026-08-31 then $3 / $15. Replaced Sonnet 4.6 as the default across consumer + developer surfaces the day it shipped.
- [Introducing Claude Fable 5 and Claude Mythos 5 — anthropic.com/news, 2026-06-09](https://www.anthropic.com/news/claude-fable-5-mythos-5) — model id `claude-fable-5`, Mythos-class tier above Opus 4.8, $10 / $50 per Mtok (2× Opus), high-risk-domain guardrails fall back to Opus 4.8. See also [Redeploying Claude Fable 5 — anthropic.com/news, 2026-06-30](https://www.anthropic.com/news/redeploying-fable-5) for the export-controls interlude (gated Jun 12–30 for foreign-national access; now redeployed).

### Why it's relevant
`.harness-profile` today reads (line 30–31):

```yaml
model:
  primary: claude-opus-4-7
  fallback: claude-sonnet-4-6
```

with a comment on line 26 that anchors the `effort_default` derivation to Anthropic's 2026-04-23 postmortem ("Claude Code defaults Opus-4.7 users to xhigh"). Since May 28 the vendor default for Claude Code's Opus users has been **Opus 4.8**, and since June 30 the vendor default for Sonnet users has been **Sonnet 5**. Sonnet 4.6 is no longer the default *anywhere*; it's a deprecated fallback.

Fable 5 is a separate case. It's real, it's shipping, and it's positioned above Opus. But it's priced at $10 / $50 per Mtok (2× Opus) and the model itself falls back to Opus 4.8 in high-risk domains. It is **not the right choice for a baseline** — it's the choice for the hardest specs a maintainer explicitly reaches for. Document it, don't default to it.

### Concrete change

**`.harness-profile`** (lines 26–33):

```diff
-# Aligned with Anthropic's 2026-04-23 postmortem (Claude Code defaults Opus-4.7 users to xhigh).
+# Aligned with Anthropic's 2026-04-23 postmortem + Opus 4.8 launch (2026-05-28) — Claude
+# Code defaults Opus users to xhigh. Update the `primary`/`fallback` pins when the vendor
+# ships a new generation and Claude Code moves its default (2026-05-28: Opus 4.7 → 4.8;
+# 2026-06-30: Sonnet 4.6 → Sonnet 5).
 # You may override `effort_default` by editing it directly — derivation only applies on first write.
 # `effort_cost_multiplier` is optional and reserved for future /tokens consumption (orchestrator ignores if absent).
 model:
-  primary: claude-opus-4-7
-  fallback: claude-sonnet-4-6
+  primary: claude-opus-4-8            # bumped 2026-07-25: Opus 4.8 shipped 2026-05-28
+  fallback: claude-sonnet-5           # bumped 2026-07-25: Sonnet 5 shipped 2026-06-30
+  # opt_in_highest: claude-fable-5    # Mythos-class tier; 2× Opus price; reach for it
+                                      # explicitly on the hardest specs, not as baseline
   effort_default: xhigh   # derived from stakes.level: medium
```

**`skills/project-init/SKILL.md`** (the model-pin template block around line 195–210): mirror the same pins so new project inits pick up Opus 4.8 + Sonnet 5, with the same `opt_in_highest` comment.

**No orchestrator change.** `.claude/agents/orchestrator.md` routes by `effort_default` and stakes, not by exact model id — the routing rules stay valid across the bump. Verify anyway (`grep -n "opus-4-7\|sonnet-4-6" .claude/agents/orchestrator.md` should return zero after the edit).

### Expected payoff
Newly-initialized projects get vendor-current defaults. Existing profiles are unaffected (project-init derivation runs on first write only — line 27 note). One-file bump per pin; the risk is limited to the `effort_default: xhigh` re-anchoring staying true for Opus 4.8, which it does (Anthropic's launch post says Opus 4.8 continues to default users to a high-effort setting on Claude Code).

### Why this isn't a duplicate
The 2026-04-19 §1 tracked the Opus 4.6 → 4.7 bump. The 2026-04-27 §1 tracked the corresponding `effort_default: high` → `xhigh` re-anchoring. Both are separate, one-model-generation bumps. This one is the 4.7 → 4.8 (and Sonnet 4.6 → Sonnet 5) follow-through — no prior § covers it (`grep -rn "claude-opus-4-8\|claude-sonnet-5" .` returns zero today).

**Verify before applying:**
1. `grep -n "claude-opus-4-7\|claude-sonnet-4-6" /home/user/claude-harness/.harness-profile /home/user/claude-harness/skills/project-init/SKILL.md` — must still return the old pins; if either has already been bumped, this § is stale.
2. Fetch [https://www.anthropic.com/news/claude-opus-4-8](https://www.anthropic.com/news/claude-opus-4-8) and [https://www.anthropic.com/news/claude-sonnet-5](https://www.anthropic.com/news/claude-sonnet-5); confirm both model ids (`claude-opus-4-8`, `claude-sonnet-5`) and that Claude Code is still on the corresponding vendor default. If Anthropic has moved on to Opus 4.9 or Sonnet 6 by the time this is applied, bump to the current generation and update the source URLs.
3. Check that no `.harness-state/` receipt schema mentions the specific model ids (grep `.harness-state/` for `opus-4-7\|sonnet-4-6`) — if it does, the bump needs a schema-preserving path, not a plain string swap.

**Recommended verdict:** apply — mechanical two-file bump, follows the exact template of the 2026-04-19 §1 upgrade, and closes a 60-day drift between the harness's prescribed defaults and the vendor's live defaults.
**Status:** PENDING — awaiting triage in PR review

---

## 2. Cite "The new rules of context engineering for Claude 5" in README "Why Not Superpowers"

**Source:** [The new rules of context engineering for Claude 5 generation models — claude.com/blog, 2026-07-24](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models) — post reports Anthropic removed ~80% of Claude Code's system prompt for advanced models, and reframes context engineering as *system prompt · Skills · CLAUDE.md · memory* rather than mostly-prompt. Companion data point: Anthropic's 2026 Agentic Coding Trends Report claims teams with well-maintained context files see 40% fewer errors and 55% faster task completion.

### Why it's relevant
`README.md:23–34` — the "Why Not Superpowers / Heavy Skill Systems?" section — is the harness's central thesis. It already cites the 2026-04-23 postmortem (line 30) as the freshest first-party evidence that adding instructions to the harness can degrade behavior. The July 24 post is fresher, more direct, and cited from Anthropic's own coding team: **they cut Claude 5-generation Claude Code's system prompt by ~80%**, because context that isn't provably useful is now a regression. That's the same argument the harness makes for its <500-token session-start footprint.

Grep confirms zero current coverage:

```
grep -rn "context-engineering\|context engineering" /home/user/claude-harness/README.md
```

returns nothing today.

### Concrete change

**`README.md`** (lines 23–34, "Why Not Superpowers" section — insert one new bullet immediately after the existing April-23 postmortem bullet and one new bullet after "Vendor primitives keep absorbing harness territory"):

```diff
 - **System-prompt regressions are subtle and slow to detect**: Anthropic's own [April 23 postmortem](https://www.anthropic.com/engineering/a-postmortem-of-three-recent-issues) traced a month-long Claude Code quality drop to three harness-layer changes — including a 25-word verbosity cap that knocked 3% off internal coding evals. Confirms first-party that adding instructions to the harness can degrade behavior.
+- **Anthropic themselves keep cutting**: The [Jul 24 context-engineering post](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models) reports Anthropic removed ~80% of Claude Code's system prompt for Claude 5-generation models, on the argument that context which isn't provably load-bearing has become a regression. That's the same knife this harness has been sharpening; the harness's ~500-token session-start footprint is aligned with where the vendor is pointing.
 - **Anthropic's own conclusion**: Their harness research removed sprint decomposition, reduced evaluator rounds, and simplified the generator — scaffolding that was essential for earlier models became counterproductive
```

And append one bullet to the **References** section (`README.md:1109–1118`):

```diff
 - [How We Built Our Multi-Agent Research System](https://www.anthropic.com/engineering/multi-agent-research-system) — Orchestrator-worker patterns, model routing
+- [The new rules of context engineering for Claude 5 generation models](https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models) — Anthropic on removing ~80% of Claude Code's system prompt for Claude 5-gen models (Jul 2026)
 - [Context Window Visualization](https://code.claude.com/docs/en/context-window) — Understanding what consumes your context
```

### Expected payoff
Fresher first-party evidence for the harness's central design decision, in the exact paragraph a reader ends up in when they ask *"why doesn't this harness ship 22k tokens of skills like superpowers?"* One bullet + one reference; no structural change.

### Why this isn't a duplicate
The 2026-04-25/26 postmortem bullets and the "Vendor primitives" bullet are about *harness-layer regressions* and *vendor-provided replacements*, respectively. This bullet is about *the vendor themselves shrinking their own system prompt in the exact direction the harness advocates* — a different claim.

**Verify before applying:**
1. `grep -rn "context-engineering-for-claude-5\|The new rules of context engineering" /home/user/claude-harness/README.md` — must return nothing today.
2. Confirm the post's ~80% figure is still the headline claim if pricing/percentage numbers have moved on quarter-later revisits.
3. `grep -n "Why Not Superpowers" /home/user/claude-harness/README.md` — should land on line 23; if the section has been renamed or restructured, adapt the diff to the current layout.

**Recommended verdict:** apply — one paragraph + one reference line; the citation is load-bearing for the harness's identity paragraph.
**Status:** PENDING — awaiting triage in PR review

---

## 3. Frame the Safety Check Skills as the harness's verification loops, citing the Jul 22 post

**Source:** [Building verification loops in Claude Code with skills — claude.com/blog, 2026-07-22](https://claude.com/blog/building-verification-loops-in-claude-code-with-skills) — post reframes an "agentic coding loop" as *ask → gather context → act → verify → maybe loop*, and argues the verification step should be a **skill Claude invokes**, not a manual check the operator does after the fact. Deterministic signals (type-checkers, linters, tests, runtime errors) go in the skill; manual "does this look right?" checks become skills too.

### Why it's relevant
The harness's `skills/` directory already ships four skills that are *exactly* this pattern (`README.md:60–63`):

- `deploy-check` — verifies env vars, secrets, rollback plan, smoke test, monitoring
- `api-smoke-test` — generates + runs curl/jq test scripts against a live URL
- `migration-check` — reviews DB migration files for reversibility, lock behavior, backfill safety, concurrent indexing
- `a11y-check` — runs axe-core via Playwright, reports WCAG violations

`README.md:330–386` groups them under **### Safety Check Skills** with no framing bullet explaining *what makes them a group*. A reader who has read the Jul 22 post lands on that header expecting "verification loops"; a reader who hasn't lands on "Safety Check Skills" and has to infer the shape. One framing sentence bridges both audiences.

### Concrete change

**`README.md`** (line 330 — replace the bare header with header + framing sentence + link):

```diff
-### Safety Check Skills
+### Safety Check Skills — verification loops
+
+These skills implement the *verify* step of the agentic coding loop described in [Building verification loops in Claude Code with skills (Jul 2026)](https://claude.com/blog/building-verification-loops-in-claude-code-with-skills): manual checks the operator would run at the end (env vars, smoke test, migration safety, a11y) turned into skills Claude invokes as part of the loop. Each takes the deterministic thing an operator would eyeball and gives it back as a repeatable, agent-callable pass.
```

Optional second touch: append the same post to the References section (`README.md:1109–1118`) as one bullet.

### Expected payoff
A reader who has just read the Jul 22 post recognizes the pattern immediately. A reader who hasn't gets the shape explained in one sentence *and* a link to the fuller treatment. Zero behavior change; the skills are already the pattern.

### Why this isn't a duplicate
The current section header is a category label without a thesis. The existing 2026-05-08 §1 (Auto Mode) is about permission classifiers. The 2026-05-10 §1 (Code Review) is about post-push cloud review. Neither frames the harness's own verification-skill lineup with a first-party name for the pattern.

**Verify before applying:**
1. `grep -n "verification loop\|building-verification-loops" /home/user/claude-harness/README.md` — must return nothing today.
2. `grep -n "^### Safety Check Skills" /home/user/claude-harness/README.md` — must still exist as a section header at ~line 330; if it's been restructured, adapt the diff.
3. Confirm the four listed skills still exist under `skills/` and are still described the same way; if any has been renamed or removed, adjust the framing sentence accordingly.

**Recommended verdict:** apply — one sentence + reference; makes the section legible to first-time readers and banks a first-party citation for a pattern the harness already implements.
**Status:** PENDING — awaiting triage in PR review

---

## 4. Cite Krieger's "large-scale code migrations" post as first-party evidence for the wave / planning-loop discipline

**Source:** [How Anthropic runs large-scale code migrations with Claude Code — claude.com/blog, 2026-07-16](https://claude.com/blog/ai-code-migration) — Mike Krieger (co-lead, Anthropic Labs) migrated a Python codebase to 165,000 lines of TypeScript across a weekend using: **hundreds of subagents**, **eight phase gates**, **three adversarial review rounds**, and **a parity harness** that diffed every command's output against the Python original. Krieger discarded the entire output of the first two runs and only kept the third; he then had Claude generate additional end-to-end tests and repair failures over four nights. Companion artifact: [anthropics/code-migration-kit-with-claude-code](https://github.com/anthropics/code-migration-kit-with-claude-code) (prompts + templates + scripts).

### Why it's relevant
The harness's `/planning-loop` runs specs through Codex adversarial review capped at 3 rounds; `/run-wave` dispatches phase-gated tasks; the receipt schema tracks per-wave completion; `parking_lot.md` catches spillover. The Krieger piece is **first-party corroboration** that phase gates + adversarial review + capped rounds + a parity check are what production-scale Claude Code migrations look like — with a specific "discard the first two runs" detail that anticipates the harness's own bias toward writing rulings to receipts rather than trusting a single-shot output.

There's no existing citation of the migration piece in the repo (`grep -rn "ai-code-migration\|Krieger\|large-scale.*migration" /home/user/claude-harness/README.md /home/user/claude-harness/AGENTS.md` returns nothing).

### Concrete change

**`README.md` References section** (`README.md:1109–1118`) — append one bullet:

```diff
 - [How We Built Our Multi-Agent Research System](https://www.anthropic.com/engineering/multi-agent-research-system) — Orchestrator-worker patterns, model routing
+- [How Anthropic runs large-scale code migrations with Claude Code](https://claude.com/blog/ai-code-migration) — Krieger's parity harness + 8 phase gates + 3 adversarial review rounds; first-party evidence for the wave/planning-loop discipline (Jul 2026). Companion kit: [anthropics/code-migration-kit-with-claude-code](https://github.com/anthropics/code-migration-kit-with-claude-code).
 - [Context Window Visualization](https://code.claude.com/docs/en/context-window) — Understanding what consumes your context
```

Optional deeper touch (out of scope for a `/micro`, worth banking as a future spec): could `planning-loop`'s cap-at-3-rounds behavior explicitly cite Krieger's discard-first-two-runs discipline? Deferred — the exact mapping isn't obvious without a design pass.

### Expected payoff
One bullet, one link — banks a load-bearing first-party citation that the wave / planning-loop shape wasn't invented in this repo but matches how Anthropic's own labs team runs their migrations. Reduces the "is this over-engineered?" objection the next time a maintainer or reader asks.

### Why this isn't a duplicate
References currently cites [Harness Design for Long-Running Apps](https://www.anthropic.com/engineering/harness-design-long-running-apps) and [Building Effective Agents](https://www.anthropic.com/engineering/building-effective-agents) — both design pieces, both older. The Krieger piece is a **case study** with a specific harness shape and specific numeric constants (3 rounds, 8 gates, 165K lines) that the harness's own shape rhymes with. Different citation shape, different weight.

**Verify before applying:**
1. `grep -rn "ai-code-migration\|Krieger.*migration\|code-migration-kit" /home/user/claude-harness/README.md` — must return nothing today.
2. Confirm `anthropics/code-migration-kit-with-claude-code` is still the canonical companion repo (Anthropic may have moved it into `anthropics/code-samples` or similar by then).
3. If the post is behind a paywall / SSO by the time this is applied, prefer the archived / mirror link.

**Recommended verdict:** apply — one bullet, high-signal first-party citation for a pattern the harness already implements.
**Status:** PENDING — awaiting triage in PR review

---

## 5. Add a "trust-prompt race" security note to setup-harness and INSTALL-FOR-COLLEAGUE.md

**Source:** [How we contain Claude across products — anthropic.com/engineering, May 2026](https://www.anthropic.com/engineering/how-we-contain-claude) — documents (among other blast-radius reductions) a live vulnerability class: when a developer cloned a repository, the project `.claude/settings.json` — including its `PreToolUse` hooks — was loaded **before** the trust-prompt appeared. A hook committed by an attacker would run automatically. Fix landed at the Claude Code layer; the general lesson (a repo you don't yet trust can execute hooks) generalizes to any harness that ships `.claude/settings.json` snippets.

### Why it's relevant
`setup-harness` installs a `PreToolUse` catastrophic-command denylist hook globally (`~/.claude/settings.json`; see `skills/_shared/loop/safety/hook-probe.ts:14–17`) and touches project-level `.claude/settings.json` for opt-in permissions. `INSTALL-FOR-COLLEAGUE.md` is the doc a first-time colleague reads when they clone the harness. Neither today mentions the trust-prompt-race class of risk. A one-paragraph note that says *"don't open a repo you don't trust in Claude Code — the `.claude/settings.json` hooks will fire before the trust prompt"* is cheap insurance and links back to Anthropic's own writeup.

Grep confirms zero current coverage:

```
grep -rn "trust prompt\|trust-prompt\|how-we-contain-claude\|settings.*before.*consent" /home/user/claude-harness
```

returns nothing.

### Concrete change

**`skills/setup-harness/SKILL.md`** — near the top of the skill body (before any file-mutating step), add a **Trust boundary** callout:

```md
> **Trust boundary — a repo you don't trust yet.** Anthropic's [May 2026 containment writeup](https://www.anthropic.com/engineering/how-we-contain-claude) documents a `PreToolUse`-hook-runs-before-trust-prompt class of risk: cloning a repo with a hostile `.claude/settings.json` can execute the attacker's hooks before you see the trust prompt. `setup-harness` writes to your global `~/.claude/settings.json` and to the project's `.claude/settings.json`. Only run it on repos whose hooks you've already read. If you're onboarding a colleague's repo you haven't audited, read `.claude/settings.json` in a plain editor first.
```

**`INSTALL-FOR-COLLEAGUE.md`** — mirror the same paragraph near the top of the "before you run" section (the file's exact structure needs a re-read before diffing).

Optional follow-through (deferred): a `setup-harness` preflight that greps `.claude/settings.json` for `PreToolUse` hook commands and prints them for operator review before appending anything. Belongs behind a `/spec-planner` design pass.

### Expected payoff
Turns a live vulnerability class into a one-paragraph guardrail in exactly the two docs a first-time user reads. Cost: two paragraphs. Payoff: a colleague onboarding this harness on an untrusted machine sees the risk before they run `setup-harness`.

### Why this isn't a duplicate
The 2026-05-08 §1 (Auto Mode) covers permission-prompt UX. Nothing in the harness today covers pre-consent hook execution. The current `setup-harness` doc talks about *what setup-harness writes* but not *what it might trigger from an untrusted repo*.

**Verify before applying:**
1. `grep -rn "trust prompt\|how-we-contain-claude\|trust boundary" /home/user/claude-harness/skills/setup-harness/SKILL.md /home/user/claude-harness/INSTALL-FOR-COLLEAGUE.md` — must return nothing today.
2. Re-read [https://www.anthropic.com/engineering/how-we-contain-claude](https://www.anthropic.com/engineering/how-we-contain-claude) and confirm the trust-prompt-race section is still there (Anthropic may have folded it into a broader containment doc).
3. Confirm Claude Code has *not* silently fixed the race by making the trust prompt block hook loading (the fix would make the callout historical rather than current). If it has, reframe the paragraph as *background* rather than *live risk*.

**Recommended verdict:** apply — two-doc, one-paragraph safety note; cheap, high-signal, and closes the "you didn't tell me" gap for onboarding.
**Status:** PENDING — awaiting triage in PR review

---

## 6. Enrich spec-planner's discovery phase with the Unknowns four-quadrant framework

**Source:** [A field guide to Claude Fable 5: Finding your unknowns — claude.com/blog, 2026-07-06](https://claude.com/blog/a-field-guide-to-claude-fable-finding-your-unknowns) — Thariq Shihipar (Claude Code team) publishes an 8-technique playbook (before / during / after implementation) grounded in a four-quadrant framing: **Known Knowns** (what's in your prompt), **Known Unknowns** (questions you know you haven't answered), **Unknown Knowns** (things obvious to you but not written down), **Unknown Unknowns** (blind spots). Argument: with Fable 5, output quality is bottlenecked by the operator's ability to surface their own unknowns.

### Why it's relevant
`.claude/agents/spec-planner.md` runs 1–3 rounds of discovery questions before writing a spec. The Unknowns framework is a **compact vocabulary** for what a discovery interview is actually trying to do: convert Known Unknowns into Known Knowns, surface Unknown Knowns (the "obvious but unwritten" tacit knowledge), and probe for Unknown Unknowns. The 8-technique playbook could inform spec-planner's interview script, its "did I ask enough?" self-check, or a new `references/unknowns-playbook.md` snapshot the agent reads before drafting.

### Concrete change (sketch — deferred)
The exact intervention isn't obvious enough to `/micro`:

- **Option A:** append a single "before you finalize, ask yourself: which quadrant are your remaining questions in?" self-check to spec-planner's discovery close-out.
- **Option B:** vendor `references/unknowns-playbook.md` (a summary of the 8 techniques, in the same shape as `references/anthropic-skill-authoring.md`) and have spec-planner read it before its 2nd round of questions.
- **Option C:** hold off on spec-planner and route through a new `/find-my-unknowns` skill that runs the framework once against a prose blob, output feeds spec-planner.

Each option touches different files with different downstream implications. Ship as a spec, not a micro.

### Expected payoff
Better discovery questions → less spec churn → fewer planning-loop rounds. But the intervention shape is unclear and the current spec-planner already has a decent question loop, so the marginal gain per option isn't obviously worth the design cost. Defer until either (a) a `/spec-planner` design pass has budget or (b) a real planning-loop miss traces back to a Known-Unknown that discovery should have caught.

**Verify before applying:**
1. `grep -rn "Unknown Unknown\|four-quadrant\|Shihipar\|unknowns playbook" /home/user/claude-harness/.claude/agents/spec-planner.md /home/user/claude-harness/skills/planning-loop /home/user/claude-harness/skills/project-init` — must return nothing today.
2. Re-read `.claude/agents/spec-planner.md`'s discovery block and re-decide between Option A / B / C given the file's current shape.
3. Fetch [https://claude.com/blog/a-field-guide-to-claude-fable-finding-your-unknowns](https://claude.com/blog/a-field-guide-to-claude-fable-finding-your-unknowns) and confirm the four-quadrant framing is still the load-bearing frame (Thariq may have shipped a v2 by then).

**Recommended verdict:** defer until `/spec-planner` design pass or a discovery-driven planning-loop miss — the framework is compelling, the implementation shape is fuzzy, and the current spec-planner is not obviously the bottleneck.
**Status:** PENDING — awaiting triage in PR review

---

## 7. Cross-check /run-loop's implicit loop taxonomy against the 4-type split from "Getting started with loops"

**Source:** [Loop engineering: Getting started with loops — claude.com/blog, ~2026-06-30 / 2026-07-01](https://claude.com/blog/getting-started-with-loops) — canonical Anthropic taxonomy for agent loops:
1. **Turn-based** — user prompts, agent responds, human decides next turn
2. **Goal-based** — agent runs to a defined stop condition (goal met / cap reached)
3. **Time-based** — agent runs on a schedule (cron / interval)
4. **Proactive** — agent watches an external signal and fires when it changes

Companion: Boris Cherny (head of Claude Code) is credited with reframing prompting-vs-loop-design as **loop engineering** — "my job is to write loops."

### Why it's relevant
The harness ships three loop-shaped skills:

- `/planning-loop` — **turn-based** (spec ⇄ Codex, capped at 3 rounds)
- `/run-loop waves` / `/run-loop issues` — **goal-based** (drain until source is empty or cap fires)
- (this very routine — the `anthropic-reviews` /loop schedule — is **time-based**, but that loop lives in the operator's crontab, not the repo)

The harness has no **proactive** loop today (no external-signal watcher). The taxonomy is a shared vocabulary that maps cleanly, without prescribing a new loop.

### Concrete change (deferred)
Two possible ways to bank this vocabulary, neither obviously worth a standalone `/micro`:

- **Option A:** one bullet at the top of `skills/run-loop/SKILL.md` and `skills/planning-loop/SKILL.md` classifying each skill against the 4-type taxonomy, linking to the post. Marginal readability gain; risks becoming stale if Anthropic renames the categories.
- **Option B:** one bullet in `AGENTS.md § Loop protocol` naming the taxonomy as background. Same risk, same marginal gain.

Better to hold the vocabulary in mind and use it the next time either loop skill needs a substantive edit, than to add prose today.

### Expected payoff
Shared vocabulary with the vendor's post. Real payoff only materializes if a future consumer project asks *"which of your loops should I use for X?"* and the answer becomes easier to write.

**Verify before applying:**
1. `grep -rn "turn-based\|goal-based\|time-based\|proactive.*loop\|Loop Engineering\|loop engineering" /home/user/claude-harness/skills/run-loop /home/user/claude-harness/skills/planning-loop /home/user/claude-harness/AGENTS.md` — must return nothing today (0 hits confirmed at generation time).
2. Fetch [https://claude.com/blog/getting-started-with-loops](https://claude.com/blog/getting-started-with-loops) and confirm the 4-type split is still the canonical vocabulary before quoting the labels.
3. Check whether Claude Code has since shipped a first-party `/loop` builder that uses the same taxonomy — if so, cite it instead.

**Recommended verdict:** defer — no concrete change earns its keep today; keep the taxonomy in reach for the next `/run-loop` or `/planning-loop` iteration.
**Status:** PENDING — awaiting triage in PR review
