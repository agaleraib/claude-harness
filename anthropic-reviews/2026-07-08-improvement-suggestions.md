# 2026-07-08 — Improvement suggestions from Anthropic posts

Generated by the "review Anthropic posts" routine. Each section links to the original post and proposes a concrete change inside this repo. See `reviewed-posts.md` for the full review log (including posts that were judged out-of-scope).

> **Context for this run.** Window covered: 2026-05-11 → 2026-07-08 (since the prior tracker entry on 2026-05-10). This is a 59-day window because the routine has not fired since Code w/ Claude 2026 (SF/London/Tokyo, May 6 / May 19 / June 10). Two headline posts land squarely on the harness's ontology: **"A harness for every task: dynamic workflows in Claude Code"** (June 2) and **"Steering Claude Code: CLAUDE.md files, skills, hooks, rules, subagents and more"** (June 18) — both name concepts this repo already builds around. Anthropic also shipped two model bumps in-window (**Opus 4.8** on May 28, **Sonnet 5** on June 30) that leave `.harness-profile`'s `model.primary: claude-opus-4-7` / `model.fallback: claude-sonnet-4-6` and the README's "Opus 4.7" prose demonstrably stale. Rounding out: a **Government of Alberta case study** (July 6) documents a 466M-LOC, ~50-parallel-agent Claude Code rollout — fresh evidence for the README's "Why Not Superpowers" thesis. Everything else in-window is skipped with reasons in the tracker.

## Triage summary

| § | Suggestion | Recommended | Why |
|---|------------|-------------|-----|
| 1 | Bump model pins Opus 4.7 → 4.8 and Sonnet 4.6 → 5 across `.harness-profile`, `skills/project-init/SKILL.md`, and README prose | apply | Two model releases (May 28 + June 30) leave the harness's canonical model pins and README prose demonstrably stale. Same pattern as applied 2026-04-19 §1 (4.6→4.7); precedent is fresh. |
| 2 | Cite "A harness for every task: dynamic workflows in Claude Code" (June 2) in README References + one paragraph on how the harness's static skills complement Anthropic's on-demand dynamic workflows | apply | Anthropic's post uses the exact same "harness" framing as this repo. `ultracode` is now baked into the Claude Code system prompt; documenting the interaction forestalls a "static harness is now redundant" question. |
| 3 | Cite "Steering Claude Code" (June 18) in README References — Anthropic-canonical seven-primitive taxonomy that maps 1:1 onto what the harness already ships | apply | This is the first Anthropic post that names CLAUDE.md / skills / subagents / hooks / rules / output-styles / append-system-prompt as one coherent set. The harness teaches four of the seven; the reference makes the ontology discoverable. Bookkeeping delta: the harness has **no output-styles or rules artifact today** — flagged for maintainer awareness only, not proposed as work. |
| 4 | Append the Alberta rollout to the README "Why Not Superpowers" evidence list — one bullet, one sentence | apply | Real production evidence (466M LOC in 20h with ~50 parallel Claude Code agents, minimal CLAUDE.md scaffolding) is exactly the kind of external corroboration the "lean context, native capability" thesis needs. Cheap doc edit; same shape as the postmortem-evidence citations added on 2026-04-25 § 1 and 2026-04-28 § 1. |

**Counts:** 4 apply · 0 defer · 0 reject · 0 spec

---

## 1. Bump model pins Opus 4.7 → 4.8 and Sonnet 4.6 → 5

**Source:** [Introducing Claude Opus 4.8 — Anthropic News, 2026-05-28](https://www.anthropic.com/news/claude-opus-4-8) and [Introducing Claude Sonnet 5 — Anthropic News, 2026-06-30](https://www.anthropic.com/news/claude-sonnet-5).

### Why it's relevant
Same shape as 2026-04-19 §1 (Opus 4.6 → 4.7), which shipped as commit `623a242`. Two GA model releases have landed since:

- **Opus 4.8** (May 28) — Anthropic reports "approximately four times less likely to allow flaws in code it has written to pass without flagging them" than 4.7, plus a `fast` mode running at 2.5× speed at 3× lower cost than prior fast modes. Fast mode is now surfaced as an in-CLI `/fast` toggle, which the Claude Code system prompt in this very session references (see the session-prompt snippet: *"Fast mode for Claude Code uses Claude Opus with faster output ... available on Opus 4.8/4.7"*).
- **Sonnet 5** (June 30) — pitched as "the most agentic Sonnet yet," ~1.0–1.35× tokenizer expansion (same warning Anthropic gave for the 4.6 → 4.7 bump — the tokenizer note the 2026-04-19 §1 spec added still applies verbatim).

The harness's canonical model pins are demonstrably stale today:

- `.harness-profile:30`: `primary: claude-opus-4-7`
- `.harness-profile:31`: `fallback: claude-sonnet-4-6`
- `skills/project-init/SKILL.md:152`: default `claude-opus-4-7`
- `skills/project-init/SKILL.md:153`: default `claude-sonnet-4-6`
- `skills/project-init/SKILL.md:206–207`: same defaults in the emitted YAML block
- `README.md:3, 10, 25, 30, 32, 852, 891`: "Opus 4.7" prose

The 2026-04-27 §1 rework that aligned `effort_default: xhigh` with Anthropic's product default remains correct for 4.8 (Opus 4.8's fast mode does not change the recommended effort baseline; it's a speed/cost knob orthogonal to reasoning effort). Nothing needs to move there.

### Concrete change

**1a. `.harness-profile:30–31` — bump both pins:**

```diff
 model:
-  primary: claude-opus-4-7
-  fallback: claude-sonnet-4-6
+  primary: claude-opus-4-8
+  fallback: claude-sonnet-5
   effort_default: xhigh   # derived from stakes.level: medium
   effort_cost_multiplier: {}   # optional; keys ∈ {low, medium, high, xhigh} → numeric (reserved for /tokens)
```

**1b. `skills/project-init/SKILL.md:152–153, 206–207` — bump defaults (four instances):**

```diff
-| `model.primary` | yes | `claude-opus-4-7` | Top-of-stack model the orchestrator dispatches to. |
-| `model.fallback` | yes | `claude-sonnet-4-6` | Used when the orchestrator demotes for cost/latency. |
+| `model.primary` | yes | `claude-opus-4-8` | Top-of-stack model the orchestrator dispatches to. |
+| `model.fallback` | yes | `claude-sonnet-5` | Used when the orchestrator demotes for cost/latency. |
```

```diff
 model:
-  primary: claude-opus-4-7
-  fallback: claude-sonnet-4-6
+  primary: claude-opus-4-8
+  fallback: claude-sonnet-5
   effort_default: [high|xhigh]          # derived from stakes.level per table above
```

**1c. `README.md:3, 10, 25, 30, 32, 852, 891` — swap "Opus 4.7" → "Opus 4.8" in prose (7 instances). Same text-swap treatment applied in 2026-04-19 §1 commit `623a242`. One sample:**

```diff
- A lean agent harness for Claude Code, designed for Opus 4.7's native capabilities.
+ A lean agent harness for Claude Code, designed for Opus 4.8's native capabilities.
```

**1d. `.claude/agents/orchestrator.md` — update model-routing decision rows** to reference `opus-4.8` / `sonnet-5` / `haiku-4.5` if they currently reference `opus-4.7` / `sonnet-4.6`. Verify locally (see step 3 in the verify block).

**1e. Older spec cross-references (`docs/waves/wave1-*`, `docs/specs/2026-04-19-harness-model-pin-and-effort-routing.md`) are durable historical records — do NOT rewrite** past specs/waves. The 2026-04-19 spec is history; its `claude-opus-4-7` references are accurate for the moment they were written. Only bump the *live* config surface: `.harness-profile`, `skills/project-init/SKILL.md`, `README.md`, and (if needed) `.claude/agents/orchestrator.md`.

### Expected payoff
Keeps the harness's core claim — "tuned for the current Opus" — true. Anyone reading the README first sees the current model. Anyone running `/setup-harness` in a new project gets a `.harness-profile` that doesn't emit stale defaults. Orchestrator routing continues to work (the model names are consumed as strings by Claude Code's model-picker; no downstream tooling in this repo hard-codes the string beyond the four surfaces listed).

### Why this isn't `defer` or `spec`
The prior 4.6 → 4.7 bump shipped as a mix of `APPLIED` (text-swap parts, §1a/§1c) and `SPEC'd` (schema-change parts, §1b/§1d, tracked in `docs/specs/2026-04-19-harness-model-pin-and-effort-routing.md`). The schema itself is now in place — this run does NOT reopen the schema question. Only the pinned string values change, which is a mechanical text-swap. `/micro` fits.

### Risks and honesty check

- **Sonnet 5's tokenizer changed.** Anthropic notes ~1.0–1.35× input token expansion. `.harness-profile`'s `tokenizer_note:` field (added by the 2026-04-19 §1 spec) already captures this warning shape — verify it survives the pin bump and, if the value string still references "4.6", update to note "Sonnet 5 tokenizer ~1.0–1.35× vs 4.6."
- **Model IDs.** Confirm exact strings against `platform.claude.com/docs/en/about-claude/models/overview` before committing — this run assumes `claude-opus-4-8` and `claude-sonnet-5`, matching the pattern of prior IDs, but Anthropic's canonical model-ID list is the authority. Do not commit if either string doesn't match.
- **`/fast` toggle.** Opus 4.8 introduces `/fast` at the CLI. If a consumer project ever wants fast mode routed by the orchestrator, that would be a separate § — not folded into this pin bump.

**Verify before applying:**
1. `grep -n "claude-opus-4-7\|claude-sonnet-4-6\|Opus 4\.7" /home/user/claude-harness/README.md /home/user/claude-harness/.harness-profile /home/user/claude-harness/skills/project-init/SKILL.md` — must return hits today; if it returns nothing, the bump has already landed and this § is stale.
2. `grep -rn "opus-4.8\|sonnet-5\|Opus 4\.8" /home/user/claude-harness --include="*.md" --include=".harness-profile"` — must return nothing today; if it returns hits, this § is partially applied and needs re-scoping.
3. Confirm `claude-opus-4-8` and `claude-sonnet-5` are the exact canonical model IDs by reading Anthropic's [models overview](https://platform.claude.com/docs/en/about-claude/models/overview) before committing the pin change. If either ID differs (dash/underscore convention, minor version suffix), align to the canonical string — do not commit a guessed ID.
4. `.claude/agents/orchestrator.md` — verify whether it references specific model names before proposing the §1d edit. If it uses generic tiers (`opus`/`sonnet`/`haiku`) already, §1d is a no-op.

**Recommended verdict:** apply — mechanical text-swap on live config surfaces, same shape as the 2026-04-19 §1 APPLIED commit, no schema change. Precedent is documented and fresh (still under a year old); if the maintainer's cadence is "bump on GA," this is overdue.
**Status:** PENDING — awaiting triage in PR review

---

## 2. Cite "A harness for every task: dynamic workflows in Claude Code" in README + document the static/dynamic interaction

**Source:** [A harness for every task: dynamic workflows in Claude Code — claude.com/blog, 2026-06-02](https://claude.com/blog/a-harness-for-every-task-dynamic-workflows-in-claude-code)

### Why it's relevant
Anthropic's post uses *the exact word this repo uses*: **harness**. Key claim from the post: "*Claude can now write its own harness on the fly, custom-built for the task in front of it, instead of always running inside the one default harness shipped for coding.*" It introduces:

- A `Workflow` tool that spawns and orchestrates parallel subagents from a JavaScript script written on demand
- The `ultracode` trigger word — an opt-in that flips Claude into "author and run a workflow for every substantive task by default"
- Failure modes the workflows fix: agentic laziness, self-preferential bias, goal drift after compaction

The Claude Code session prompt in this very session (see the `Workflow` tool block) already includes the `ultracode` instruction, so this is already in the Claude Code default configuration — the harness is silent on how it interacts. Today, `grep -rn "ultracode\|dynamic workflow" /home/user/claude-harness` returns **zero hits**.

Two open questions the harness needs a stance on:

1. **When is a `Workflow` the right escape hatch?** The harness ships hand-designed multi-agent patterns: `orchestrator` dispatches sonnet/haiku subagents; `generator` + `code-reviewer` runs a build/review loop; `spec-planner` → build → review is the spec pipeline. These are static compositions. Anthropic's Workflow tool is the dynamic equivalent — Claude authors a fresh pipeline per task.
2. **Do the harness's skills clash with `ultracode`?** If a skill instruction says "run `/commit`" but `ultracode` is on, Claude may instead author a bespoke workflow. Prior art: 2026-05-10 §1 (Code Review / `/ultrareview`) named the complementarity of local vs. cloud reviewers. Same shape applies here.

### Concrete change

**2a. Append one bullet to `README.md:1109` References section:**

```diff
   - [How We Built Our Multi-Agent Research System](https://www.anthropic.com/engineering/multi-agent-research-system) — Orchestrator-worker patterns, model routing
+  - [A harness for every task: dynamic workflows in Claude Code](https://claude.com/blog/a-harness-for-every-task-dynamic-workflows-in-claude-code) — Anthropic's on-demand `Workflow` tool + the `ultracode` trigger; complementary to this harness's static agent/skill compositions
   - [Context Window Visualization](https://code.claude.com/docs/en/context-window) — Understanding what consumes your context
```

**2b. Append one short paragraph to `README.md:1080` "Multi-agent coordination (2026 trend)" section, immediately before "Human checkpoints (by design):":**

```diff
 - `project-tracker` + `claude-progress.txt` → hand off between sessions

+#### Static harness vs. Anthropic's dynamic workflows
+As of [June 2 2026](https://claude.com/blog/a-harness-for-every-task-dynamic-workflows-in-claude-code), Claude Code ships a `Workflow` tool that lets Claude *author* a bespoke multi-agent pipeline per task (opt-in via the `ultracode` trigger). This harness ships *pre-designed* pipelines — `spec-planner` → build → `code-reviewer` for feature work, `orchestrator` for model-tier routing, `generator` + `code-reviewer` + `ui-evaluator` for sustained builds. They are complementary: use the harness's static pipelines when the shape of the work is known (spec-driven feature waves, PR review before commit, session-start/session-end rituals); reach for `Workflow` / `ultracode` when the work is a one-shot fan-out whose shape is discovered at runtime (mass migration, exhaustive audit, cross-repo sweep). The static pipelines encode "*this is how we work on this project*"; dynamic workflows encode "*Claude, plan the parallelism yourself.*"
+
 Human checkpoints (by design):
```

### Expected payoff
A future reader (or future-you) who asks "does `ultracode` obsolete the `orchestrator` agent?" hits the answer in the README. Same style as the 2026-05-10 §1 note that positioned `/ultrareview` alongside the local `code-reviewer` — no behavioral change, just an honest naming of the layering. Also unblocks a future maintainer decision: if the harness ever wants to opt some skills *into* `ultracode` (a `/spec-planner` that auto-fans-out on `ultracode`, say), the README naming makes that a delta rather than a rewrite.

### Why this isn't `spec`
No new artifact, no new schema. Two documentation edits, both single-block. `/micro` fits.

### Why this isn't a duplicate of 2026-05-10 §1
2026-05-10 §1 was about `/ultrareview` and hosted Code Review — two *reviewer* products that overlap with the `code-reviewer` agent. This § is about `Workflow` and `ultracode` — the *orchestration* primitive that overlaps with `orchestrator` and `spec-planner`. Different agent, different product surface, different pattern. Both § can coexist and reference each other.

**Verify before applying:**
1. `grep -rn "ultracode\|a-harness-for-every-task\|dynamic workflow" /home/user/claude-harness` — must return zero hits today. If it returns hits, either 2026-05-10 §1's paragraph has been expanded to cover this ground (unlikely — that § was scoped to reviewers) or another run has landed this suggestion first. Re-check before writing.
2. Confirm the `Workflow` tool is still available in the default Claude Code session at apply time — the tool is scoped to certain Claude Code versions. If it's since been rolled back or renamed, drop the paragraph's `Workflow` reference and keep only the `ultracode` reference.
3. Confirm `ultracode` is still the trigger word (not renamed to something else). The Claude Code system prompt is the authoritative source at apply time.

**Recommended verdict:** apply — pure documentation, one reference bullet + one paragraph, closes the same class of "static vs. Anthropic-hosted" gap that 2026-05-10 §1 closed for reviewers.
**Status:** PENDING — awaiting triage in PR review

---

## 3. Cite "Steering Claude Code" in README References — canonical seven-primitive taxonomy

**Source:** [Steering Claude Code: CLAUDE.md files, skills, hooks, rules, subagents and more — claude.com/blog, 2026-06-18](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more)

### Why it's relevant
Anthropic's post canonicalizes a **seven-primitive taxonomy** for steering Claude Code behavior:

| # | Primitive | Harness surface today | Notes |
|---|-----------|-----------------------|-------|
| 1 | `CLAUDE.md` files | Yes — `README.md:750–772` "Write Your CLAUDE.md" section + `examples/*/CLAUDE.md` templates | Documented as "lay of the land," matching the post's framing |
| 2 | Skills (`.claude/skills/` + `SKILL.md`) | Yes — 22 skills in `skills/`, symlinked to `~/.claude/skills/` | The harness is largely a skills bundle |
| 3 | Subagents (`.claude/agents/`) | Yes — 6 agents in `.claude/agents/` | `code-reviewer`, `orchestrator`, `spec-planner`, `generator`, `ui-evaluator`, `project-tracker` |
| 4 | Hooks | Yes — discussed in `README.md:891`, denylist described in `CLAUDE.md`'s Loop-protocol section | Global git `post-commit` hook noted at `README.md:475` |
| 5 | Rules (`.claude/rules/`) | **No** | Not surfaced anywhere in the repo |
| 6 | Output styles (`.claude/output-styles/`) | **No** | Not surfaced anywhere in the repo |
| 7 | Append system prompt (per-invocation) | **No** | Not surfaced; not typically a harness-author's tool |

The harness teaches primitives 1–4 well. The remaining three (rules, output-styles, append-system-prompt) are absent. That's not necessarily a bug — the post itself frames rules and output-styles as **session-scoped** and **format-focused** respectively, which is closer to a per-project preference than a harness convention. But it's information the maintainer should surface consciously rather than accidentally.

`grep -rn "steering-claude-code\|Steering Claude Code\|output_style\|output-style" /home/user/claude-harness` today returns **zero hits**.

### Concrete change

**3a. Append one bullet to `README.md:1109` References section:**

```diff
   - [A harness for every task: dynamic workflows in Claude Code](...) — [from §2 above]
+  - [Steering Claude Code: CLAUDE.md files, skills, hooks, rules, subagents and more](https://claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more) — Anthropic's canonical seven-primitive taxonomy for steering Claude Code (this harness teaches CLAUDE.md, skills, subagents, hooks — the other three primitives are documented Claude Code features left unclaimed by the harness by design)
   - [Context Window Visualization](https://code.claude.com/docs/en/context-window) — Understanding what consumes your context
```

**3b. (Bookkeeping only, no code change this run.)** The three primitives the harness *doesn't* claim — rules, output-styles, appendSystemPrompt — are surfaced here so the maintainer can decide, per primitive:

- **Rules** are session-scoped constraints. The harness's `.harness-state/today_goal.md` and `current_micro.md` already play a similar role at a per-day/per-block granularity. If a consumer project wanted per-session behavioral constraints, `.claude/rules/` would be the Claude-Code-native surface. No action recommended in this § — decision belongs with the maintainer.
- **Output styles** control format/tone. The harness's `criteria/` files are close but different: `criteria/` scores against rubrics; output-styles change presentation. Interesting-but-not-obviously-needed for a coding harness.
- **appendSystemPrompt** is per-invocation and Claude-Code-specific. Not a natural harness-author surface — not proposed for adoption.

If the maintainer wants any of these primitives to enter the harness's scope, that's a separate § (a `spec` verdict, not `apply`).

### Expected payoff
Two effects:

1. **Readable ontology.** A future reader who wants to understand *how the harness relates to Claude Code's steering primitives* has a single canonical Anthropic reference to compare against. Same value as the existing References entries for `Best Practices` and `Custom Subagents`.
2. **Surfaces a deliberate omission.** The harness's silence on output-styles/rules/appendSystemPrompt is either (a) deliberate ("solo tooling, don't need these") or (b) unexamined. Adding the reference makes the omission auditable — future-you sees the four vs. three split and either accepts it or opens a new §.

### Why this isn't `spec`
No new artifact, no new primitive. Single reference bullet.

**Verify before applying:**
1. `grep -rn "steering-claude-code\|Steering Claude Code" /home/user/claude-harness` — must return zero hits today. If it returns hits, this § is stale.
2. `grep -rn "output_style\|output-style\|\.claude/rules/" /home/user/claude-harness` — should return zero hits today. If it returns hits, the maintainer has landed one of the three unclaimed primitives since this suggestion was written, and the bullet's "left unclaimed by design" phrasing needs adjustment.
3. Confirm the post URL slug hasn't been shortened / rewritten by Anthropic (blog URLs occasionally get renamed). The canonical is `claude.com/blog/steering-claude-code-skills-hooks-rules-subagents-and-more` as of this run.

**Recommended verdict:** apply — pure documentation, one reference bullet, plus a bookkeeping delta the maintainer can either accept or fork off into a spec later.
**Status:** PENDING — awaiting triage in PR review

---

## 4. Append the Alberta rollout to README "Why Not Superpowers" evidence list

**Source:** [Government of Alberta uses Claude to find and fix cybersecurity vulnerabilities across government systems — anthropic.com/news, 2026-07-06](https://www.anthropic.com/news/alberta-government-claude-cybersecurity)

### Why it's relevant
The Alberta Ministry of Technology and Innovation, in production, used Claude Code (Opus + Sonnet) with roughly **50 agents running autonomously and in parallel** to scan **466 million lines of code across 1,280 applications and 3,400 code repositories in 20 hours** — versus an estimated ~6.5 human-years otherwise. The scan produced auditable findings citing exact file+line, generated fixes, wrote missing test coverage, and shipped patches.

The pattern is **exactly the shape the harness advocates**:

- Multiple parallel agents (mirrors the `orchestrator` fan-out pattern and the `run-loop` engine)
- Two-stage per-repo pipeline: rules-engine scan → agent review + citation (mirrors the harness's `code-reviewer` + criteria-scoring shape)
- Minimal per-repo scaffolding (no evidence of custom-CLAUDE.md-per-repo or heavy Superpowers-style skill loading)

The README's "Why Not Superpowers / Heavy Skill Systems?" section (`README.md:23–34`) currently cites: the Superpowers GitHub Issue #190, two Claude Code issues about instruction ignoring, AGENTbench research, the April 23 postmortem, Anthropic's own harness-research conclusions, and the vendor-primitive-absorption note. Alberta is the first public **production-scale** external corroboration.

Prior art: 2026-04-25 §1 (`bake the "no verbosity caps" rule`) and 2026-04-28 §1 (`add the April 23 postmortem to Why-Not-Superpowers`) both APPLIED evidence bullets in this exact section for exactly this reason. This is the same shape.

### Concrete change

Append one bullet to `README.md:24–34` (the "Why Not Superpowers" list), immediately after the "Vendor primitives keep absorbing harness territory" bullet:

```diff
 - **Vendor primitives keep absorbing harness territory**: Claude Code now ships a built-in `/security-review` slash command, and Anthropic launched [Claude Security in public beta on 2026-04-30](https://claude.com/blog/claude-security-public-beta) with full-codebase vulnerability scanning + patch generation on Opus 4.7. A custom security-review skill would have been a reasonable addition to this harness six months ago; today it's redundant with first-party tooling. The pattern repeats — keep the harness lean so vendor releases don't deprecate it.
+- **Production evidence at scale**: The Government of Alberta ran ~50 parallel Claude Code agents against **466M lines across 3,400 repos in 20 hours**, generating auditable findings + fixes + tests — using a two-stage per-repo pipeline (rules-engine scan → agent review with cited file+line) that mirrors this harness's `code-reviewer`-against-`criteria/` shape ([Anthropic case study, 2026-07-06](https://www.anthropic.com/news/alberta-government-claude-cybersecurity)). Corroborates that lean-context, native-capability harnesses scale to production without Superpowers-style skill loading.

 **This harness keeps context overhead under 500 tokens at session start** (agent descriptions only). Full agent/skill content loads only when invoked.
```

### Expected payoff
Fresh, dated, non-Anthropic-authored production evidence — a public-sector customer, not an Anthropic-internal engineering post — that the lean-harness pattern the README argues for is what actually gets used in a 3,400-repo rollout. The reader who arrives skeptical of the "keep it lean" thesis has one more real-world anchor to weigh.

### Why this isn't `defer`
Same shape as the postmortem-evidence bullets applied in 2026-04-25 §1 and 2026-04-28 §1 — one sentence, one URL, in a section explicitly designed to accumulate evidence over time. The `defer` verdict on 2026-05-10 §3 (dreaming/Managed Agents) was because that suggestion was speculative and platform-conditional; this one is a citation of what already shipped.

### Risks and honesty check

- **Anthropic-authored source.** The customer story lives on `anthropic.com/news`, so it's a first-party framing of a third-party rollout. Not neutral. The bullet's phrasing should stay factual (LOC scanned, hours taken, pattern shape) and not editorialize on causal claims Anthropic makes about their model. The proposed diff above does that.
- **The two-stage pipeline shape.** The claim "mirrors this harness's `code-reviewer`-against-`criteria/` shape" is a genuine parallel but not a strong one — Alberta's rules engine is not the same as `criteria/` scoring. If a reader digs into the case study, the parallel is *directional* not *exact*. The phrasing in the diff says "mirrors ... shape" (weak claim), not "uses this harness's pattern" (strong claim it can't support).

**Verify before applying:**
1. `grep -n "Alberta\|466M\|466 million" /home/user/claude-harness/README.md` — must return zero hits today. If it returns hits, this § has already landed.
2. Re-read [the Alberta case study](https://www.anthropic.com/news/alberta-government-claude-cybersecurity) at apply time to confirm the 466M / 3,400 / 20h figures haven't been corrected. If any number differs, use the correction, not this document's copy.
3. Confirm the "Why Not Superpowers" section is still structured as a bullet list at apply time. If the maintainer has since re-shaped that section into paragraphs, adapt the diff to fit.

**Recommended verdict:** apply — one evidence bullet, same shape as two APPLIED prior citations in the same section, zero risk.
**Status:** PENDING — awaiting triage in PR review

---

## Summary

Four §s out of ~30 in-window Anthropic posts. **§1 (apply)** is the highest-priority: two model bumps left the harness's canonical model pins demonstrably stale for 41–70 days. **§2 (apply)** and **§3 (apply)** are one-paragraph-each documentation refreshes that add three Anthropic references — "A harness for every task," "Steering Claude Code," and the seven-primitive taxonomy — to the README, closing the same class of "static-harness-vs-Anthropic-hosted" gap that 2026-05-10 §1 closed for reviewers. **§4 (apply)** appends the Alberta case study to the README's Why-Not-Superpowers evidence list — the first production-scale, non-Anthropic-authored external corroboration of the lean-context thesis, same shape as the postmortem-evidence bullets already applied on 2026-04-25 §1 and 2026-04-28 §1. All four sections are `/micro`-shaped: no new artifacts, no new schemas, no new agents, no new skills. Highest-leverage to ship first: §1 (the pin bump is the only one where staleness silently propagates to consumer projects via `setup-harness`). Everything else in-window (Fable 5 / Mythos 5 releases and redeployment, Series H funding, Widening frontier AI conversations, Higher usage limits + SpaceX, Anthropic Institute posts, When AI builds itself, How we contain Claude across products [companion to already-applied 2026-05-08 §1], Claude Science workbench, Bringing Claude Code to government [FedRAMP], Alberta Cowork announcements, Claude Cowork mobile+web, CodeRabbit customer story, Running an AI-native engineering org, Unreasonable effectiveness of HTML, Economic Index Cadences) is tracked as `skipped` with reasons in `reviewed-posts.md`.
