{
  "_comment": "Paste the 'filesystem' entry below into the 'mcpServers' block of Claude Desktop's MCP config (~/Library/Application Support/Claude/claude_desktop_config.json on macOS). Generated by /new-cowork {{AREA}} {{PROJECT}} on {{TODAY}}.",
  "_caveat": "No programmatic Project-creation API exists for Claude Desktop or claude.ai as of 2026-05-13 — the file drag + this MCP config paste is a one-time operator action.",
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "{{COWORK_PATH}}",
        "{{MEMORY_ROOT}}"
      ]
    }
  }
}
