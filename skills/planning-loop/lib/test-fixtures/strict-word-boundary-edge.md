# strict-word-boundary-edge fixture — cleanup/fastener must NOT trip clean/fast

The single bullet contains `cleanup` and `fastener` (no real lexicon token) and
names no mechanism, so it is `no-mechanism`, NOT `unbound-judgment`.

**Acceptance criteria:**
- [ ] The cleanup routine and the fastener assembly are both documented.

## End
