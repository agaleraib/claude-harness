# strict-mixed-counts fixture — 2 strict, 1 sub-strict (strict=2 total=3)

**Acceptance criteria:**
- [ ] `GET /api/x` returns `200`.
- [ ] The write path stays < 300 ms.
- [ ] The system handles everything gracefully.

## End
