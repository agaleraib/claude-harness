# strict-incidental-span-darkmode fixture — a bare-phrase span does not bind a judgment bullet

The `dark mode` span is only alphanumerics and a space (not command-shaped), so
under the M1-binding rule it does NOT bind the `clean` judgment word; the bullet
stays unbound-judgment (strict=0 total=1).

**Acceptance criteria:**
- [ ] The `dark mode` toggle looks clean.

## End
