# strict-no-requirements fixture — no acceptance-criteria block at all

## Overview
This spec deliberately omits the acceptance-criteria marker, so the scanner
reports strict=0 total=0 and exits 0 (no crash).

## Out of Scope
Nothing here either.
