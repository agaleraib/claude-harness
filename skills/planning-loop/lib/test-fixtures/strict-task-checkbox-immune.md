# strict-task-checkbox-immune fixture — Implementation-Plan checkboxes are out of scope

There is no `**Acceptance criteria` marker anywhere, so no block ever opens and
the judgment-word Task checkbox below is never scanned (total=0).

## Implementation Plan (Sprint Contracts)

### Phase 1
- [ ] **Task 1:** Make the shutdown clean and graceful.
  - **Files:** foo.sh
  - **Verify:** `bash foo.sh` exits 0.

## Out of Scope
Nothing.
